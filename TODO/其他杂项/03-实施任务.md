# 实施任务：其他杂项

## 功能点 fp-001：Application.yml 配置测试计划

- [ ] 准备测试环境（Java 17+, Maven 3.6+, Node.js 18+）
- [ ] 检查 8080 端口是否被占用
- [ ] 编译后端（Maven compile）
- [ ] 启动后端并验证健康状态
- [ ] 安装前端依赖并启动 Electron
- [ ] 测试剪藏功能
- [ ] 测试待办功能
- [ ] 测试周报功能
- [ ] 测试配置功能
- [ ] 生成测试报告

## 功能点 fp-002：React 文件清理与文档更新

- [ ] 删除 `frontend/src/` 整个目录
- [ ] 删除 `frontend/node_modules/` 目录
- [ ] 删除 `frontend/public/` 目录
- [ ] 删除 `frontend/package.json` 和 `frontend/package-lock.json`
- [ ] 删除 `frontend/vite.config.js` 和 `frontend/eslint.config.js`
- [ ] 删除 `frontend/README.md`
- [ ] 更新 PRD.md 技术栈描述（4.1/4.2/4.3）
- [ ] 更新 README.md 技术栈和 Git 同步功能说明

## 功能点 fp-003：修复后端状态同步与工作台产品开发页 Bug

- [ ] 后端 HealthController 新增 `@CrossOrigin(origins = "*")` 和 `@GetMapping("/api/health")`
- [ ] 删除 editor.html 底部后端状态指示器 UI（#backendStatusBtn, #startBackendBtn 等）
- [ ] 删除 editor.js 中后端状态管理代码段（BACKEND_STATE, setBackendState, startBackendHealthCheck 等）
- [ ] 确认删除后 showToast 等其它逻辑正常使用
- [ ] index.html 新增 `handleBackendReady()` 和 `refreshAllSubPages()` 函数
- [ ] 统一三处 onBackendReady 回调为调用 `handleBackendReady()`
- [ ] 为子 iframe 添加 load 事件监听补发刷新
- [ ] 所有子页面引入 `js/logger.js`
- [ ] 补全 refresh 消息监听（knowledge.html, wiki.html, pdf.html, data-observability.html, settings.html, workspace.html）
- [ ] 修复 `.product-dev-view` 为 `position: fixed; inset: 0; z-index: 60`
- [ ] 新增 `.pd-tab` 点击切换逻辑（参照 .detail-tab 写法）

## 功能点 fp-004：系统初始化窗口页面重构

- [ ] main.js 首次运行窗口添加 `frame: false`
- [ ] main.js 设置窗口添加 `frame: false`
- [ ] 修改首次运行判断逻辑为仅检查 `config.configured`
- [ ] config.html 新增自定义标题栏（.titlebar 可拖拽区域 + 关闭按钮）
- [ ] config.html 精简页面为仅保留端口配置和存储目录两个区块
- [ ] config.html 简化首次运行欢迎横幅文案
- [ ] config.html 移除 AI API Key 必填校验
- [ ] config.html 移除子目录结构展示和 updateDerivedPaths 函数
- [ ] settings.html 补充 DeepSeek API Key 申请链接
- [ ] settings.html 补充 DashScope API Key 申请链接（如无）

## 功能点 fp-005：宠物图标 AI 生成及多动作上传改造

- [ ] 使用 GenerateImage 生成 24 张默认图标（4 角色 × 6 动作）
- [ ] 保存图片到 `frontend/assets/mascot/{character_id}/{action}.png`
- [ ] settings.js 新增 `MASCOT_ACTIONS` 常量数组
- [ ] settings.js 将 `iconDataUrl` 改为 `iconDataUrls` 对象
- [ ] settings.js 新增 `buildMascotImageUrl()` 函数
- [ ] settings.js 更新默认配置为 `iconType: 'preset-images'`, `iconId: 'luoxiaohei'`
- [ ] settings.html 替换单文件上传为 6 个独立上传区域
- [ ] settings.js 新增 `handleMascotMultiUpload()` 处理多动作上传
- [ ] settings.js 更新 `handleMascotPreset()` 和 `renderMascotPreview()`
- [ ] editor.js 更新 `applyMascotPreference()` 支持 preset-images 和 upload 类型
- [ ] editor.css 补充 `.ai-pet-image` 动作动画（wave/jump/think/sleep/celebrate）
- [ ] 清理旧代码（删除 buildMascotSvg 中的预设 SVG，保留兜底 fallback）

## 功能点 fp-006：密码管理器深度集成

- [ ] 创建 `DesEncryptionUtil.java`（DES 加解密 + Key 生成 + 校验）
- [ ] 创建 `PasswordEntry.java` 和 `VaultData.java` 实体
- [ ] 创建 `VaultRequest.java` 和 `VaultResponse.java` DTO
- [ ] 创建 `PasswordVaultService.java`（核心业务逻辑）
- [ ] 创建 `PasswordVaultController.java`（REST API，11 个端点）
- [ ] 创建 `frontend/vault.html`（页面 HTML + CSS）
- [ ] 创建 `frontend/vault.js`（交互逻辑）
- [ ] index.html 导航栏新增「密码」按钮
- [ ] index.html SPA 路由新增 vault 视图
- [ ] main.js 托盘菜单新增「密码管理」入口
- [ ] main.js 实现自动锁定逻辑（最小化到托盘/闲置 5 分钟）

## 功能点 fp-007：右键菜单增强

- [ ] editor.js 新增 `showDictLibAddDialog()` 函数（自定义模态对话框替代 prompt）
- [ ] editor.js 修改 `promptAddToDictLib()` 调用新对话框
- [ ] editor.js 新增全局变量 `pendingDictAdd`
- [ ] editor.js 修改 `aiSearch` 设置待处理标记
- [ ] editor.js 修改 `onlineTranslateText` 设置待处理标记
- [ ] editor.js 在 `sendAiMessage` 的 SSE done 事件中插入自动添加词典逻辑
- [ ] editor.html 右键菜单重新分组排序（编辑操作/AI功能/翻译功能/词典管理）
- [ ] editor.html 补齐缺失图标（📋✂️📄🔍🔎🧠📖🌐📝📚）
- [ ] editor.js 更新分隔线显隐逻辑（3 条分隔线，按选中状态显隐）
- [ ] dict-offline.js DICT_CN 新增约 200 条词汇（常用/软件开发/财务金融）

## 功能点 fp-008：周报生成 Skill 设计

- [ ] 修改 todo.html 待办创建 placeholder 为编码格式引导
- [ ] 创建 `.trae/skills/weekly-report-gen/SKILL.md`
- [ ] 配置 TRAE Schedule（cron: `0 16 * * 5`, timezone: Asia/Shanghai）
- [ ] 手动验证周报生成端到端流程

## 功能点 fp-009：数据观测模块优化

- [ ] DataObservabilityController.java 移除 `POST /api/data/action-events` 接口
- [ ] DataObservabilityController.java 移除 `GET /api/data/habits` 接口
- [ ] DataObservabilityController.java 移除 `GET /api/data/insights` 接口
- [ ] trends() 响应增加 `skippedLineCount` 字段
- [ ] EventTypes.java 移除 `BUTTON_CLICKED` 常量
- [ ] data-observability.html 移除常用分类/常用标签/最近动作/使用说明面板
- [ ] data-observability.html 诊断摘要改为折叠/展开（默认收起）
- [ ] data-observability.html 顶部统计卡片从 5 张精简为 3 张
- [ ] data-observability.js 不再请求 `/habits` 和 `/insights`，移除相关渲染函数
- [ ] workspace.html 移除 `trackFunctionClick` 函数及 42 处 `data-func-tag` 属性
- [ ] 更新 DataObservabilityControllerTest 适配新接口

## 功能点 fp-010：异常日志模块设计

- [ ] 创建 `ExceptionLogWriter.java`（底层文件写入器，按天滚动）
- [ ] 创建 `ExceptionLogService.java`（Spring Service，封装写入器，提供 record() 方法）
- [ ] 创建 `GlobalExceptionHandler.java`（@ControllerAdvice 全局异常捕获）
- [ ] DataObservabilityController.java 新增异常日志查询/统计/清理 API
- [ ] 修改 `electron/logger.js` 新增异常日志写入 tmp 目录功能
- [ ] 修改 `electron/main.js` 启动时通过 IPC 获取 storagePath，初始化异常日志路径
- [ ] 修改 `electron/main.js` 将 uncaughtException/unhandledRejection 处理改为写入统一异常日志
- [ ] 修改 `frontend/js/logger.js`，error 级别增加向后端 POST 上报
- [ ] data-observability.html 新增异常日志面板 HTML 结构
- [ ] data-observability.js 实现异常日志列表渲染、筛选、详情弹窗、趋势图、清理功能