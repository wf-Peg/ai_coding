# 验收清单：其他杂项

## 功能点 fp-001：Application.yml 配置测试计划

- [ ] 后端能正常编译并启动，API 端点响应正常
- [ ] 前端 Electron 能正常启动，界面加载正常
- [ ] 剪藏功能测试通过
- [ ] 待办功能测试通过
- [ ] 周报功能测试通过
- [ ] 配置保存和加载功能测试通过
- [ ] 测试报告完整生成

## 功能点 fp-002：React 文件清理与文档更新

- [ ] 所有 React 相关文件已删除（src/, node_modules/, public/, package.json, vite.config.js, eslint.config.js）
- [ ] 当前使用的 HTML 文件不受影响（index.html, clip.html, todo.html）
- [ ] PRD.md 技术栈描述已更新（React+Vite → HTML5+CSS3+JS）
- [ ] README.md 已更新，包含 Git 同步功能说明
- [ ] 前端结构为纯 HTML/CSS/JS，无未使用的依赖

## 功能点 fp-003：修复后端状态同步与工作台产品开发页 Bug

- [ ] 后端编译通过（`mvn compile` 无错误）
- [ ] 访问 `http://127.0.0.1:8081/api/health` 返回 200 `{status:"UP"}`
- [ ] 访问 `http://127.0.0.1:8081/health` 仍正常返回
- [ ] 编辑器状态栏不再出现「后端启动态/启动后端」按钮
- [ ] 开发者工具 Network 中不再出现 `api/health` 500/CORS 报错
- [ ] 后端就绪后，各子页面数据自动刷新（无需手动刷新）
- [ ] 工作台→产品开发，页面整屏显示（无需滚动）
- [ ] 产品开发 Tab（总览/需求看板/知识图谱/时间线/归档）点击可正常切换
- [ ] 异常日志中可看到 source=frontend 的记录

## 功能点 fp-004：系统初始化窗口页面重构

- [ ] 初始化窗口无原生边框，顶部显示自定义标题栏（渐变色）
- [ ] 自定义标题栏可拖拽，关闭按钮正常
- [ ] 页面中无邮件通知、软件更新、开机自启、子目录结构展示等区块
- [ ] 选择 DeepSeek → 提示横幅显示 DeepSeek 注册链接
- [ ] 选择 DashScope → 提示横幅显示阿里云 API Key 链接
- [ ] 设置窗口同样无原生边框，自定义标题栏正常
- [ ] 保存并启动功能正常

## 功能点 fp-005：宠物图标 AI 生成及多动作上传改造

- [ ] 24 张图片已生成到 `frontend/assets/mascot/` 目录
- [ ] 设置页四个预设卡片能正常显示对应角色的默认动作图
- [ ] 切换预设，编辑器底部宠物按钮图标更新为对应角色的 PNG
- [ ] 上传 6 张自定义 PNG（每个动作各一张），各动作对应图片正确
- [ ] 切换不同动作，编辑器按钮显示对应动作图片
- [ ] CSS 动画（thinking 快速奔跑、sleeping 打盹、error 抖动等）在 `<img>` 上正常生效
- [ ] 上传超过 2MB 的 PNG 时被拒绝并提示
- [ ] 非 PNG 文件上传时被拒绝

## 功能点 fp-006：密码管理器深度集成

- [ ] 导航栏出现「密码」入口
- [ ] 点击进入显示锁屏页
- [ ] 点击「生成新 Key」→ 后端返回 Base64 Key → 展示给用户
- [ ] 输入 Key → 解锁 → 密码库初始化成功，显示空列表
- [ ] 新增密码条目 → 保存 → DES 加密写入 vault.enc
- [ ] 重新打开应用 → 输入相同 Key → 解锁 → 看到已保存的条目
- [ ] 输入错误 Key → 解锁失败，提示「Key 不正确」
- [ ] 密码生成器 → 调整参数 → 生成密码 → 使用
- [ ] 安全审计 → 检测到弱密码/重复密码 → 显示警告
- [ ] 最小化到托盘 → 自动锁定 → 恢复后需重新输入 Key

## 功能点 fp-007：右键菜单增强

- [ ] 右键选中文本 → 「添加词典库」→ 弹出输入框 → 输入翻译 → 确认 → 提示添加成功
- [ ] 右键选中文本 → 「添加词典库」→ 弹出输入框 → 点取消/点击遮罩 → 关闭
- [ ] 右键选中中文文本 → 「AI 搜索选中内容」→ AI 回复完成后 → 自动提示已添加到自定义映射
- [ ] 右键选中中文文本 → 「在线翻译」→ AI 回复完成后 → 自动提示已添加到词典库
- [ ] 右键菜单分组正确：编辑操作 → AI 功能 → 翻译功能 → 词典管理
- [ ] 所有菜单项都有图标显示
- [ ] AI 搜索/在线翻译失败时（网络错误），不触发自动添加
- [ ] 选中软件开发相关中文（如"微服务"）→ 离线翻译 → 显示正确英文翻译
- [ ] 选中财务金融相关中文（如"市盈率"）→ 离线翻译 → 显示正确英文翻译

## 功能点 fp-008：周报生成 Skill 设计

- [ ] todo.html 待办创建 placeholder 显示编码格式引导
- [ ] 创建新待办 `测试任务[需求分析,4h,50%]` 能保存成功
- [ ] 调用 skill 生成周报，xlsx 文件生成在正确目录
- [ ] 文件名格式为 `开发组_周报_{本周五日期}_彭文峰.xlsx`
- [ ] Sheet 1 包含工作待办（不含生活待办）
- [ ] Sheet 2 按 taskType 分组汇总正确
- [ ] Sheet 3 包含进行中的待办作为下周计划
- [ ] xlsx 格式未被破坏（合并单元格、列宽、字体样式）
- [ ] TRAE Schedule 配置正确

## 功能点 fp-009：数据观测模块优化

- [ ] `mvn compile` 无错误
- [ ] `mvn test` 全部通过
- [ ] 观测页正常加载，不报 404/500
- [ ] 移除的接口不再被前端请求（Network 无 /habits, /insights, /action-events 请求）
- [ ] 工作台页面功能按钮正常响应，控制台无异常
- [ ] 异常日志面板正常查询和展示
- [ ] 诊断摘要面板默认收起，可展开查看详细文件级信息

## 功能点 fp-010：异常日志模块设计

- [ ] 后端启动后 `{clip.storage.path}/tmp/exception-logs/` 目录自动创建
- [ ] 手动触发后端异常，检查异常日志文件是否写入
- [ ] Electron 进程 uncaughtException 触发后，异常日志文件写入
- [ ] 前端 `FrontendLogger.error()` 调用后，异常日志通过后端 API 写入
- [ ] 数据观测模块异常日志面板能正确展示异常列表
- [ ] 异常日志筛选（按来源/级别/日期范围）正常
- [ ] 异常日志详情弹窗显示完整堆栈信息
- [ ] 清理功能能正确删除指定天数前的日志文件