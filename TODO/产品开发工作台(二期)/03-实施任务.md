# 产品开发工作台(二期) — 实施任务

## 执行顺序

### 第一阶段：基础修复（P0，按顺序执行）

#### 任务 1：统一约定 — 定稿 feature-points.json 字段契约

- [ ] 定稿 feature-points.json 字段定义（以 SKILL.md 为准）
- [ ] 同步修正 spec.md、SKILL.md、todo-directory-specification.md 三份文档
- [ ] 确保所有文档字段定义一致（contentFile/file、title/label、category/type、status/completed）

#### 任务 2：修复 TodoScannerService 字段解析

**涉及文件：**
- `backend/.../service/TodoScannerService.java`

**修改内容：**
- [ ] clips[].contentFile 读取键改为 `"contentFile"`（原 `"file"`）
- [ ] clips[].title 读取键改为 `"title"`（原 `"label"`）
- [ ] clips[].category 读取键改为 `"category"`（原 `"type"`）
- [ ] todos[].status 读取改为 string `"status"`（原 boolean `"completed"`）
- [ ] 修复 requirement 对象解析（String 强转 → Object 解析）
- [ ] 剪藏落库使用 config.clipCategory 和 clipDef.category，而非硬编码 `product-dev`
- [ ] 实现增量导入逻辑：.imported 存 ISO 时间戳，按 featurePoints[].id 增量
- [ ] todoDir 从 AppConfigService 派生（storagePath + "/TODO"）

#### 任务 3：合并初始化器

**涉及文件：**
- `backend/.../config/ProductDevStartupInitializer.java`（删除）
- `backend/.../service/ProductDevWorkspaceInitializer.java`（保留修改）

**修改内容：**
- [ ] 删除 ProductDevStartupInitializer.java
- [ ] 统一工作台名称：`产品开发`
- [ ] 统一工作台颜色：`#2383e2`
- [ ] 统一工作台类型：`project`（删除 Workspace.TYPES 中追加的 `product-dev`）
- [ ] 统一三条内置规则 operator：tag=equals, type=in, category=contains
- [ ] 修复 pd-builtin 表达式启动覆盖问题（仅在缺失时自愈）
- [ ] 确保启动时只扫描一次，日志清晰

#### 任务 4：重写 tasks.md / checklist.md

**涉及文件：**
- `.trae/specs/product-dev-workspace/tasks.md`
- `.trae/specs/product-dev-workspace/checklist.md`

**修改内容：**
- [ ] tasks.md 删除旧接口要求（`/api/workspace/product-dev/*`）
- [ ] tasks.md 更新为新 spec 内容（基于 workspace 规则系统）
- [ ] checklist.md 删除知识图谱、甘特图、D3.js 等二期验收项
- [ ] checklist.md 更新为当前 MVP 验收项

### 第二阶段：功能增强（P1，可并行）

#### 任务 5：前端视图交互统一

**涉及文件：**
- `frontend/workspace.html`

**修改内容：**
- [ ] 将 product-dev-view 移入 .main-area 内部（#detailView 之后，</main> 之前）
- [ ] CSS 改为普通视图显隐切换（display:none/block），删除 fixed/inset/z-index
- [ ] 引入统一 showView(view) 函数
- [ ] 绑定 pdSidebarToggle 事件
- [ ] loadProductDev() 改为调用 /api/workspace/pd-builtin/resolve
- [ ] 隐藏知识图谱和甘特图 tab

#### 任务 6：全部概览仪表盘改造

**涉及文件：**
- `frontend/workspace.html`

**修改内容：**
- [ ] overviewView 添加 dashboard-row 和 chart-row 区域
- [ ] 新增 ov- 前缀仪表盘 CSS 样式
- [ ] 新增 renderOverviewDashboard(stats) 函数
- [ ] 新增 renderOverviewCharts(data) 函数（Chart.js）
- [ ] 修改 loadOverview() 渲染逻辑

#### 任务 7：标签筛选 + 埋点 + 提示

**涉及文件：**
- `frontend/workspace.html`
- `frontend/index.html`
- `backend/.../controller/DataObservabilityController.java`

**修改内容：**
- [ ] 前端新增 activePdTag 状态变量和标签 pill 筛选条
- [ ] 实现 pill 点击本地过滤，联动所有子视图
- [ ] 后端新增 POST /api/data/action-events 接口
- [ ] 前端新增 trackFunctionClick(btn) 埋点助手
- [ ] workspace.html 和 index.html 补充 title 悬浮提示

#### 任务 8：文字标准化 + 最近活动修复

**涉及文件：**
- `frontend/workspace.html`
- `frontend/index.html`
- `backend/.../service/ProductDevWorkspaceInitializer.java`

**修改内容：**
- [ ] hero 标题：`工作台` → `全部概览`
- [ ] eyebrow：`Product Development` → `Product Overview`
- [ ] h1 标题：`产品开发工作区` → `产品概览`
- [ ] 新增 renderRecentActivities(contents) 函数
- [ ] loadOverview() 末尾调用 renderRecentActivities()
- [ ] index.html 工具提示更新
- [ ] 所有 CSS 注释和 JS 错误提示同步更新

#### 任务 9：面包屑导航 + 工作台详情入口优化

**涉及文件：**
- `frontend/workspace.html`

**修改内容：**
- [ ] detail 视图头部添加面包屑导航 HTML
- [ ] 面包屑点击事件绑定
- [ ] 修改 selectWorkspace(id)：移除 overview 过滤逻辑，统一进入 detail 视图

#### 任务 10：历史代码清理

**涉及文件：**
- 旧 ProductDevController.java（废弃/删除）
- 旧 ProductDevService.java（废弃/删除）
- 旧 ProductDevModel.java（废弃/删除）
- `frontend/workspace.html`

**修改内容：**
- [ ] 废弃或删除旧 ProductDevController/Service/Model
- [ ] 移除前端 pdHistoryMigrateBtn 和 pdNewRequirementBtn 按钮
- [ ] 移除对应事件监听器代码
- [ ] 可选：清理历史 product-dev 数据

### 第三阶段：配置与验证

#### 任务 11：配置同步

**涉及文件：**
- `backend/src/main/resources/application_templete.yml`
- `electron/main.js`

**修改内容：**
- [ ] application_templete.yml 增加 `product-dev.todo-dir` 配置项
- [ ] Electron main.js generateApplicationYml() 同步生成该配置

#### 任务 12：全链路验证

- [ ] 构造最小 feature-points.json 示例
- [ ] 归档 → 扫描 → 落库 → 规则筛选 → 前端展示 全链路验证
- [ ] 确认全链路数据一致后，铺开存量迁移