# 03-实施任务 — 知识库与Vault模块

## 一、知识库Bug修复

### 1.1 来源剪藏点击修复
| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `frontend/knowledge-detail.js` | 修改 | renderSourceClips 改为内联弹窗展示，通过 API 获取剪藏标题和摘要 |
| `frontend/knowledge-detail.html` | 修改 | 添加来源剪藏详情弹窗 HTML 结构和 CSS 样式 |

### 1.2 评论功能实现
| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `backend/.../model/Comment.java` | 新建 | 评论模型（id, knowledgeId, author, content, createdAt） |
| `backend/.../service/FileStorageService.java` | 修改 | 添加评论 JSON 文件读写方法 |
| `backend/.../controller/KnowledgeController.java` | 修改 | 添加 GET/POST /api/knowledge/{id}/comments 端点 |

### 1.3 关联知识展示优化
| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `frontend/knowledge-detail.js` | 修改 | renderLinkedKnowledge 通过 API 获取标题而非仅显示 ID |
| `frontend/knowledge-detail.html` | 修改 | 添加 wikilink 语法使用说明和知识图谱入口 |

## 二、Wikilink 渲染美化

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `frontend/knowledge-detail.js` | 修改 | 构建 titleToId 映射表，预处理 [[wikilink]] 替换为 HTML 链接；renderDetail 改为 async |
| `frontend/knowledge-detail.html` | 修改 | 添加 Obsidian 风格 wikilink CSS（三个主题适配） |

## 三、Vault 路径配置化

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `backend/.../config/WikiConfig.java` | 修改 | 注入 AppConfigService，新增 @PostConstruct resolveVaultPath() |
| `backend/.../service/sync/SourceSyncService.java` | 修改 | 移除独立 resolveVaultPath()，直接使用 wikiConfig.getVaultPath() |
| `backend/src/main/resources/application_templete.yml` | 修改 | 更新 vault-path 注释 |

## 四、密码库Bug修复 + 全局日志

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `backend/.../controller/PasswordVaultController.java` | 修改 | 所有端点增加 try-catch + 详细日志 |
| `backend/.../service/PasswordVaultService.java` | 修改 | 所有关键方法增加 log.info/log.error |
| `frontend/vault.html` | 修改 | 添加主题初始化逻辑；改进 doInit() 错误处理 |
| `electron/logger.js` | 新建 | Electron 日志模块（文件写入 + 控制台双输出，按天滚动保留30天） |
| `electron/main.js` | 修改 | 引入 logger，替换 console.log |
| `electron/preload.js` | 修改 | 新增 logToFile IPC 通道 |
| `frontend/js/logger.js` | 新建 | 前端日志工具（通过 IPC 写入文件） |

## 五、密码库导入功能修复

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `frontend/vault.html` | 修改 | 新增 .modal-submit CSS；修复 importNext() 跳转；步骤2隐藏下一步按钮；修复 deleteEntry/deleteVaultAction/switchToVault 响应检查；清理 console.error；修复 closeImportModal 清理 |
| `backend/.../service/PasswordVaultService.java` | 修改 | 删除硬编码 chrome 标签行 |

## 六、密码库导入功能（CSV + 浏览器插件）

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `backend/.../controller/PasswordVaultController.java` | 修改 | 添加 @CrossOrigin(origins = "*")；新增 POST /api/vault/import 端点 |
| `backend/.../service/PasswordVaultService.java` | 修改 | 新增 importEntries(List<PasswordEntry>) 方法 |
| `frontend/vault.html` | 修改 | 侧边栏新增导入按钮；新增导入模态框（三步向导）；新增 parseCsv() 函数 |
| `plugin/manifest.json` | 修改 | web_accessible_resources 新增 import-password.html |
| `plugin/background.js` | 修改 | 新增密码管理根菜单 + 2个子项 |
| `plugin/import-password.html` | 新建 | 弹窗页面结构 |
| `plugin/import-password.js` | 新建 | 自动填充 + 解锁检查 + 保存逻辑 |

## 七、多密码库支持

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `backend/.../service/PasswordVaultService.java` | 修改 | 新增 VaultMeta 内部类；vaults.json 注册表读写；migrateLegacyVault()；init/unlock 增加 vaultName 参数；listVaults/switchVault/deleteVault/checkKey 方法 |
| `backend/.../controller/PasswordVaultController.java` | 修改 | 新增 GET /vaults, PUT /vaults/active, DELETE /vaults/{name}, POST /check-key 端点；修改 init/unlock/status 参数和返回值 |
| `frontend/vault.html` | 修改 | 锁屏页增加密码库选择器；初始化模态框重设计；新增密码库管理模态框；新增忘记 Key 引导模态框；doUnlock 错误处理改进；侧边栏新增密码库管理入口 |

## 八、Wiki 模块框架

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `backend/.../service/wiki/BatchIngestService.java` | 已实现 | 批量入库 AI 处理 |
| `backend/.../service/wiki/VaultWatchService.java` | 已实现 | Vault 目录监听与增量入库 |
| `backend/.../service/wiki/WikiPageService.java` | 已实现 | 页面 CRUD 管理 |
| `backend/.../service/wiki/WikiIndexService.java` | 已实现 | 索引与日志维护 |
| `backend/.../service/wiki/WikiQueryService.java` | 已实现 | 知识查询服务 |
| `backend/.../service/wiki/WikiLintService.java` | 已实现 | 健康检查与 lint 报告 |
| `backend/.../service/wiki/MocGeneratorService.java` | 已实现 | MOC 索引页生成 |
| `backend/.../controller/WikiIngestController.java` | 已实现 | /api/wiki/ingest 接口 |
| `backend/.../controller/WikiLintController.java` | 已实现 | /api/wiki/lint 接口 |
| `backend/.../controller/WikiQueryController.java` | 已实现 | 查询接口 |
| `frontend/wiki.html` | 已实现 | 查询/归档/批量入库/索引/页面列表/lint 界面 |

## 九、Wiki 查询重构 + 模型档位设计

### 9.1 查询重构
| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `backend/.../service/wiki/WikiLocalRetriever.java` | 新建 | 本地拆词检索（CJK 2-gram + 英文空格分词） |
| `backend/.../service/wiki/WikiQueryService.java` | 修改 | 本地优先+LLM 兜底逻辑；三参签名+单参重载 |
| `backend/.../controller/WikiQueryController.java` | 修改 | 透传 includeClips/includeKnowledge |
| `frontend/wiki.html` | 修改 | 两个 checkbox + extraSources 渲染 |

### 9.2 模型档位设计
| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `backend/.../config/ModelConfig.java` | 修改 | 移除 simpleTierProvider/strongTierProvider，新增 simpleTierModel/strongTierModel |
| `backend/.../config/AppConfig.java` | 修改 | 同上字段替换 |
| `backend/.../service/AppConfigService.java` | 修改 | 同步逻辑更新 |
| `backend/.../llm/LlmProvider.java` | 修改 | 新增 chat(modelName, ...) 重载 |
| `backend/.../llm/DeepSeekLlmProvider.java` | 修改 | 实现 chatWithModel |
| `backend/.../llm/DashScopeLlmProvider.java` | 修改 | 实现 chatWithModel |
| `backend/.../llm/OpenAiCompatibleLlmProvider.java` | 修改 | 实现 chatWithModel |
| `backend/.../llm/RoutingLlmProvider.java` | 修改 | 档位路由改为传递模型名 |
| `frontend/settings.html` | 修改 | 两个 select 替换为 input + datalist；activeProvider 移入高级设置 |
| `frontend/js/settings.js` | 修改 | loadConfig/saveConfig 对应更新 |
| `backend/src/main/resources/application_templete.yml` | 修改 | 更新默认配置注释 |