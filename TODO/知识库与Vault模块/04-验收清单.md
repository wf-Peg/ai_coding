# 04-验收清单 — 知识库与Vault模块

## 一、知识库Bug修复

### 1.1 来源剪藏
- [ ] 创建一条知识，关联某个来源剪藏
- [ ] 打开知识详情页，点击来源剪藏
- [ ] 确认：弹出内联窗口显示剪藏标题和摘要，无导航栏重复

### 1.2 评论功能
- [ ] 打开知识详情页，评论区显示"暂无评论，来说点什么吧"
- [ ] 输入昵称和评论内容，点击"发布评论"
- [ ] 确认：评论成功发布，列表刷新显示新评论
- [ ] 再次打开该知识详情页，确认评论持久化

### 1.3 关联知识展示
- [ ] 在知识编辑器中，内容区域使用 `[[另一条知识标题]]` 语法
- [ ] 保存后，打开知识详情页，确认"关联知识"区域显示标题链接
- [ ] 点击"知识图谱"按钮，确认图谱页面显示节点和连线
- [ ] 在图谱中拖拽节点、滚轮缩放、点击节点查看详情

## 二、Wikilink 渲染美化
- [ ] 打开一条有 `[[知识标题]]` 引用的知识详情页
- [ ] 确认 `[[知识标题]]` 渲染为蓝色虚线链接样式，不再是纯文本
- [ ] 点击 wikilink，确认跳转到对应的知识详情页
- [ ] 切换 Dark/Notion 主题，确认 wikilink 颜色跟随主题变化
- [ ] 确认普通 Markdown 内容（标题、列表、代码块等）渲染不受影响
- [ ] 确认 myThoughts 区域中的 [[wikilink]] 也正确渲染

## 三、Vault 路径配置化
- [ ] mvn compile 确认后端编译通过
- [ ] 检查 WikiConfig.resolveVaultPath() 日志输出，确认路径解析正确
- [ ] 重启后端后，点击"立即同步"，确认不再报 sources directory not found
- [ ] 如果 sources/ 目录存在且包含 .md 文件，确认同步条数正确更新

## 四、密码库Bug修复 + 全局日志

### 4.1 密码库初始化
- [ ] 故意输入已存在的 vaultName → 应显示"密码库名称已存在"而非"请检查后端服务"
- [ ] 后端未启动时 → 显示"无法连接后端服务"
- [ ] 错误 DES Key → 显示"DES Key 不正确"

### 4.2 主题跟随
- [ ] 在主页面切换深色主题 → 打开 vault.html → 应显示深色主题

### 4.3 日志文件
- [ ] 启动应用 → electron.log 有启动日志
- [ ] 操作密码库 → application.log 有 vault 相关日志
- [ ] 前端操作 → electron.log 有 `[Frontend]` 前缀日志
- [ ] 历史日志自动清理（30天前自动删除）

## 五、密码库导入功能修复
- [ ] 导入密码 → 选择 CSV 文件 → 步骤2预览 → 只显示"上一步"和"确认导入"两个按钮 → 样式为紫色圆角按钮
- [ ] 步骤2点击"确认导入" → 跳转步骤3显示结果 → "完成"按钮样式正确
- [ ] 删除密码条目 → 确认弹窗 → 条目从列表消失
- [ ] 密码库管理 → 切换密码库 → 弹窗确认 → 切换到锁屏
- [ ] 密码库管理 → 删除密码库 → 弹窗确认 → 删除成功
- [ ] 导入 CSV（Bitwarden 格式）→ 检查标签为 "imported" 而非 "chrome"

## 六、密码库导入功能（CSV + 插件）

### 6.1 CSV 导入
- [ ] Chrome 导出测试 CSV → vault.html 导入 → 验证条目数正确 → 验证去重生效
- [ ] 重复导入同一 CSV → 全部标记为重复跳过
- [ ] 导入 Bitwarden CSV → 字段映射正确

### 6.2 插件右键导入
- [ ] vault 未解锁时右键 → 弹窗显示未解锁警告
- [ ] vault 已解锁时右键 → 弹窗自动填充网站信息 → 填写密码保存 → vault.html 列表出现新条目
- [ ] "打开密码库" → 浏览器新标签打开 index.html#/vault

### 6.3 跨域验证
- [ ] 插件调用 `/api/vault/status` 不被 CORS 拦截
- [ ] 插件调用 `/api/vault/entry` 成功保存

## 七、多密码库支持
- [ ] 错误提示精确：后端未启动→"无法连接后端服务"，错误 Key→"DES Key 不正确"，密码库不存在→"请先初始化"
- [ ] 自动生成 Key → 填入输入框 + 显示保存提示
- [ ] 复制 Key → toast "请保存到安全位置"
- [ ] 创建多个密码库 → 切换 → 来回解锁读取不同数据
- [ ] 删除非当前密码库 → 成功删除
- [ ] 旧版 vault.enc 在 vault/ 根目录 → 升级后自动迁移到 vault/default/ → 正常解锁

## 八、Wiki 模块框架
- [ ] sources 目录 .md 文件可批量入库生成 wiki 页面
- [ ] Vault 新文件被自动检测并触发增量入库
- [ ] index.md 正常维护页面索引
- [ ] lint-report.md 正确反映健康状态
- [ ] MOC 索引页正常生成
- [ ] wiki.html 前端操作正常（查询、归档、批量入库、索引、页面列表、lint）

## 九、Wiki 查询重构 + 模型档位设计

### 9.1 查询重构
- [ ] 本地拆词检索正常返回结果（CJK 2-gram 匹配）
- [ ] 本地检索无结果时自动切换到 LLM 兜底
- [ ] usedLocalRetrieval 标记回传前端展示
- [ ] 多数据源查询的 checkbox 正常切换 wiki/clips/knowledge 范围

### 9.2 模型档位设计
- [ ] 默认值：simpleTierModel=deepseek-v4-flash，strongTierModel=deepseek-v4-pro
- [ ] 前端两个输入框默认填充对应值，预设下拉提供 deepseek-v4-flash/v4-pro/qwen-turbo/qwen-plus 等选项
- [ ] 用户可自由输入任意模型名
- [ ] 简单任务（定位/抽取/Lint）发送的 API 请求中 model 字段为 simpleTierModel 的值
- [ ] 复杂任务（综合/矛盾检测）发送的 API 请求中 model 字段为 strongTierModel 的值
- [ ] 用户只配置了 API Key 未改模型名时，默认行为正常工作
- [ ] activeProvider 移入高级设置后，初级用户不需要关心这项配置