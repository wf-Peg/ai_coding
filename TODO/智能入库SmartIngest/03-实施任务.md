# 智能入库SmartIngest - 实施任务

## 一、实施计划总览

| 阶段 | 任务 | 预估工时 | 优先级 |
|------|------|----------|--------|
| 阶段1 | 双AI路径架构设计 | 2h | P0 |
| 阶段2 | 意图识别与字段提取规则 | 3h | P0 |
| 阶段3 | 默认流程与例外流程区分 | 2h | P0 |
| 阶段4 | 后端统一入库接口 IngestController | 4h | P0 |
| 阶段5 | 浏览器插件智能入库集成 | 3h | P1 |
| 阶段6 | 前端 clip.html 智能入库入口 | 2h | P1 |
| 阶段7 | SKILL.md 智能入库 Skill 指令文件 | 4h | P0 |
| 阶段8 | 代码审查缺陷修复 | 2h | P0 |
| 阶段9 | 代码审查改进建议 | 2h | P2 |
| 阶段10 | 话题模块开发 | 16h | P0 |

## 二、阶段1：双AI路径架构设计

### 2.1 设计要点
- Agent 路径：Skill 接收用户文本 → AI 意图识别+字段提取 → curl 调用已有接口
- 代码路径：POST /api/ingest → 后端 AI 处理 → 路由入库
- 两条路径逻辑等价，AI 计算位置不同

### 2.2 实施步骤
1. 确定 Agent 路径的完整流程和节点
2. 确定代码路径的完整流程和节点
3. 确定两条路径的规则同步机制（SKILL.md 注释提醒）
4. 文档化架构设计

## 三、阶段2：意图识别与字段提取规则

### 3.1 文件修改清单
- `backend/.../service/AiService.java`
- `.trae/skills/smart-ingest/SKILL.md`

### 3.2 实施步骤
1. AiService.java 新增 `identifyIntent(String text)` 方法
   - systemPrompt 定义 clip/todo/topic 三类意图特征
   - 返回意图类型字符串
2. AiService.java 新增 `extractFields(String text, String intent)` 方法
   - 按意图提取结构化字段（JSON 格式）
   - 无法确定的字段用 null
3. SKILL.md 定义意图识别规则（特征+示例表格）
4. SKILL.md 定义字段提取映射（意图→字段→目标 API 表格）
5. 修复 getString() 方法：trim 后若为空返回 defaultValue

### 3.3 验证
- 对三类文本测试意图识别正确性
- 测试空字符串/空字段降级处理

## 四、阶段3：默认流程与例外流程区分

### 4.1 文件修改清单
- `.trae/skills/smart-ingest/SKILL.md`

### 4.2 实施步骤
1. 新增"流程决策树"章节（ASCII 流程图）
2. 新增"强制类型关键词识别"章节（关键词→接口映射表）
3. 重写"调用后端接口"章节（默认流程接口 + 例外流程接口）
4. 新增"接口选择优先级"章节
5. 重写"使用示例"章节（标注默认流程/例外流程）
6. 同步到全局级 SKILL.md（SHA-256 验证 + BOM 检查）

### 4.3 验证
- 阅读 SKILL.md 自检：默认流程覆盖 clip/todo/topic，例外流程覆盖 4 种关键词
- 运行时验证：测试默认流程和例外流程各 2-3 个场景

## 五、阶段4：后端统一入库接口 IngestController

### 5.1 文件修改清单
- `backend/.../controller/IngestController.java`（新增）
- `backend/.../service/AiService.java`（修改）

### 5.2 实施步骤
1. 创建 IngestController.java，POST /api/ingest 端点
2. 注入 AiService，调用 identifyIntent + extractFields
3. 实现意图路由逻辑（clip→ClipService, todo→TodoService, topic→TopicService）
4. 实现降级链路（意图模糊→clip store-only；字段提取失败→原文截断）
5. 实现三种响应格式（成功/降级/失败）
6. 修复 saveAsClip 补设 capturedAt（LocalDateTime.now）
7. 编写接口文档附录

### 5.3 验证
- POST /api/ingest 传有效文本 → 返回正确 intent 和入库结果
- POST /api/ingest 传空文本 → 返回 validation 错误
- 验证 capturedAt 不为 null
- 验证降级场景的 degraded 标记

## 六、阶段5：浏览器插件智能入库集成

### 6.1 文件修改清单
- `browser-extension/options.js`
- `browser-extension/options.html`
- `browser-extension/popup.js`
- `browser-extension/popup.html`
- `browser-extension/background.js`

### 6.2 实施步骤
1. options.js DEFAULT_CONFIG 新增 ingestUrl 配置项
2. options.html 新增智能入库 API 地址配置项
3. popup.js 新增 handleSmartIngest() 函数
4. popup.js 增加空值校验和长度校验（text.length < 5）
5. popup.html 表单下方新增"智能入库"按钮
6. background.js 新增 smartIngest message handler
7. 响应适配 {success} 格式

### 6.3 验证
- 插件配置页可设置 API 地址
- 按钮点击调用 POST /api/ingest
- 输入过短文本时前端拦截提示
- 成功入库后 toast 提示

## 七、阶段6：前端 clip.html 智能入库入口

### 7.1 文件修改清单
- `frontend/clip.html`

### 7.2 实施步骤
1. textarea 旁新增"智能入库"按钮
2. 实现 smartIngestClip() 函数
3. 按钮加载态：禁用 + 文字改为"分析中..."
4. finally 块中恢复按钮
5. 移除对不存在的 #status-message 元素引用（死代码）
6. 成功 toast 提示意图类型 + 降级说明
7. 失败 toast 提示错误信息

### 7.3 验证
- 页面显示按钮，点击后有加载态
- 空内容/过短内容拦截
- 成功后 toast 显示正确信息
- 按钮完成后恢复

## 八、阶段7：SKILL.md 智能入库 Skill 指令文件

### 8.1 文件修改清单
- `.trae/skills/smart-ingest/SKILL.md`（项目级）
- `C:/Users/pengwenfeng/.trae-cn/skills/smart-ingest/SKILL.md`（全局级）

### 8.2 实施步骤
1. 创建项目级 SKILL.md，包含以下章节：
   - 概述
   - 流程决策树
   - 强制类型关键词识别
   - 意图识别规则
   - 字段提取映射
   - 调用后端接口（默认流程 + 例外流程）
   - 使用示例
   - 异常处理
   - 重试机制（3次+延迟0/2/5秒）
   - 平台兼容性（Windows PowerShell）
   - 接口选择优先级
   - 资源完整性要求
2. PowerShell 脚本同步到全局级目录
3. SHA-256 哈希对比 + BOM 检查验证

### 8.3 验证
- 项目级和全局级 SHA-256 一致
- 全局级文件无 BOM
- 关键章节齐全

## 九、阶段8：代码审查缺陷修复

### 9.1 文件修改清单
- `backend/.../controller/IngestController.java`
- `frontend/clip.html`
- `browser-extension/popup.js`

### 9.2 实施步骤
1. **修复1**：IngestController.java getString() 增加 trim + 空字符串降级
   ```java
   private String getString(Map<String, Object> fields, String key, String defaultValue) {
       Object val = fields.get(key);
       if (val == null) return defaultValue;
       String s = val.toString().trim();
       return s.isEmpty() ? defaultValue : s;
   }
   ```
2. **修复2**：IngestController.java saveAsClip 补设 capturedAt
   ```java
   request.setCapturedAt(java.time.LocalDateTime.now().toString());
   ```
3. **修复3**：clip.html smartIngestClip() 增加按钮加载态 + finally 恢复
4. **修复3**：clip.html 移除 #status-message 死代码引用
5. **修复4**：popup.js handleSmartIngest 增加 text.length < 5 长度校验

### 9.3 验证
- 修复1：POST 空标题文本 → 响应 title 为截断原文
- 修复2：入库后 capturedAt 不为 null
- 修复3：按钮点击后变为"分析中..."，完成后恢复
- 修复4：3 字符输入被拦截
- 回归：原有接口行为不变

## 十、阶段9：代码审查改进建议

### 10.1 文件修改清单
- `browser-extension/background.js`
- `.trae/skills/smart-ingest/SKILL.md`
- `backend/.../controller/IngestController.java`

### 10.2 实施步骤
1. **改进A**：background.js smartIngest 复用 apiRetryCount 配置进行重试
2. **改进B**：SKILL.md 意图规则前加同步提醒注释 `> ⚠️ 本规则需与后端 AiService.identifyIntent 的 systemPrompt 保持同步`
3. **改进C**：IngestController.java saveAsClip 响应 title 改为 clip.getTitle()
4. **改进D**：文档记录 saveAsClip 二次保存的 upsert 现状

### 10.3 验证
- 改进A：smartIngest 失败时按配置重试
- 改进B：SKILL.md 有同步提醒注释
- 改进C：响应 title 与实际存储一致

## 十一、阶段10：话题模块开发

### 11.1 文件修改清单
- `backend/.../model/Topic.java`（新增）
- `backend/.../dto/TopicRequest.java`（新增）
- `backend/.../dto/TopicResponse.java`（新增）
- `backend/.../service/TopicService.java`（新增）
- `backend/.../controller/TopicController.java`（新增）
- `backend/.../service/FileStorageService.java`（修改）
- `frontend/topic.html`（新增）
- `frontend/topic.js`（新增）
- `frontend/topic-detail.html`（新增）
- `frontend/topic-detail.js`（新增）
- `frontend/topic-editor.html`（新增）
- `frontend/topic-editor.js`（新增）
- `frontend/index.html`（修改）
- `browser-extension/background.js`（修改）
- `browser-extension/popup.html`（修改）
- `browser-extension/popup.js`（修改）
- `browser-extension/manifest.json`（修改）

### 11.2 实施步骤
1. Step 2: 创建 Topic 数据模型和 DTO
2. Step 3: 创建 TopicService 和修改 FileStorageService
3. Step 4: 创建 TopicController（8 个 REST API 端点）
4. Step 5: 创建话题列表页（topic.html + topic.js，卡片流布局）
5. Step 6: 创建话题详情页（topic-detail.html + topic-detail.js）
6. Step 7: 创建话题编辑器（topic-editor.html + topic-editor.js）
7. Step 8: 修改 index.html 导航栏增加话题入口
8. Step 9: 浏览器扩展增加"剪藏到话题"右键菜单
9. Step 10: Electron 集成验证

### 11.3 验证
- 后端 API 全部可用（CRUD + like + search + from-clip）
- 前端页面正常渲染（列表/详情/编辑器）
- 导航栏话题切换正常
- 浏览器扩展右键菜单正常
- Electron 无 CORS 问题