# 设计文档：DSH（DeepSeek Harness）集成

## 架构设计

```
┌─────────────────────────────────────────────────────┐
│                   DSH Web (Agent)                     │
│  ┌──────────────────────────────────────────────┐    │
│  │           mcp-client (stdio)                   │    │
│  │  ┌─────────────────────────────────────────┐  │    │
│  │  │ cut-shelter-bridge (MCP stdio server)   │  │    │
│  │  │ - clip_search / list / add / delete     │  │    │
│  │  │ - clip_categories                       │  │    │
│  │  │ - todo_list / add / set_status          │  │    │
│  │  │ - learning_plan_list / wiki_index       │  │    │
│  │  │ - weekly_report_status                  │  │    │
│  │  └──────────────┬──────────────────────────┘  │    │
│  │                 │ HTTP REST                     │    │
│  │  ┌──────────────▼──────────────────────────┐  │    │
│  │  │    CutShelter Backend (8081)              │  │    │
│  │  │    - clip-storage/  JSON + Markdown       │  │    │
│  │  │    - obsidian-vault/  wiki pages          │  │    │
│  │  │    - weekly-report/  reports              │  │    │
│  │  └─────────────────────────────────────────┘  │    │
│  └──────────────────────────────────────────────┘    │
│  ┌──────────────────────────────────────────────┐    │
│  │  clip-capture plugin (Phase 1)               │    │
│  │  - clip_session 工具（会话成果落库）          │    │
│  └──────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

## 技术方案

### MCP Server（桥接层）

- 基于 `@modelcontextprotocol/sdk` 的 `McpServer`
- 传输层：`StdioServerTransport`（stdio 协议）
- 每个工具封装对剪藏后端 REST API 的 HTTP 调用
- 超时控制：`AbortController`，默认 60s
- 工具定义：Zod schema 参数校验

### DSH 插件（会话落库层）

- 基于 `@deepseek-ai/dsh-tools` 的 `defineTool`
- 通过 `cordis.yml` 的 `insert` 行挂载
- 插件注册 `clip_session` 工具，调用后端 `/api/clips` 写入
- 支持 `baseUrl` 配置或环境变量覆盖

## 接口定义

### MCP 工具清单

| 工具名 | 方法 | 路径 | 参数 | 类型 |
|--------|------|------|------|------|
| clip_search | GET | /api/clips/search | query, topK | 只读 |
| clip_list | GET | /api/clips | keyword, workflowStatus, limit | 只读 |
| clip_categories | GET | /api/clips/categories | - | 只读 |
| clip_add | POST | /api/clips | content, title, summary, tags, category, source, sourceUrl, useAiTags | 写 |
| clip_delete | DELETE | /api/clips/{id} | id | 破坏性 |
| todo_list | GET | /api/todos | status, keyword, limit | 只读 |
| todo_add | POST | /api/todos | title, description, deadline, priority | 写 |
| todo_set_status | PUT | /api/todos/{id}/status | id, status | 写 |
| learning_plan_list | GET | /api/learning-plans | - | 只读 |
| wiki_index | GET | /api/wiki/index | - | 只读 |
| weekly_report_status | GET | /api/weekly-report/status | - | 只读 |

## 关键决策

1. MCP 桥 vs 直接 API 调用：选择独立 MCP 桥，避免影响后端稳定性
2. Dual-read 策略：文件工具零成本读 + MCP 工具写
3. 写操作安全：需用户确认，clip_delete 必须显式确认
4. 自动去重：clip_add 内部去重，避免重复数据
## Phase 2：Electron 内嵌 DSH sidecar（AI 干活面板）

- 固定端口 3081（避开 3080）；3081 已有实例直接复用，退出不杀用户进程。
- 运行时由主进程生成 `dsh-agent.patch.yml`（webserver 端口覆盖 + insert mcp-client/clip-capture），写入无空格配置目录，避免 shell 参数拆分。
- dsh CLI 解析优先级：配置 dshBinPath → DSH_BIN → 内置 node_modules → npx 缓存扫描（含 LOCALAPPDATA）→ npx 自动安装（pin `@deepseek-ai/dsh@0.1.0-rc.7`）。
- 进度五态广播：detecting / installing / starting / ready / failed；前端状态灯 + 进度条 + 取消/重试/刷新/反色。
- iframe 不预加载，就绪后统一 loadAgentFrame（空则设置、已设置则强制重载），2s 端口探测 watchdog 兜底收敛状态。
- 离线闭包：scripts/build-dsh-offline.mjs BFS 收集生产依赖，prebuild 生成并经 extraResources 内置为 resources/dsh-offline。
