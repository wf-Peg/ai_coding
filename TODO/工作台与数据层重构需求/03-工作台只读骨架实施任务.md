# Tasks

- [x] Task 1: 梳理并补齐工作台只读数据契约
  - [x] SubTask 1.1: 检查现有内容索引、项目索引和数据观测接口的数据结构，复用可满足工作台列表的数据。
  - [x] SubTask 1.2: 若现有接口不足，新增最小只读接口，返回内容引用、可用类型和项目摘要；禁止返回正文或执行写入。
  - [x] SubTask 1.3: 先编写后端失败测试，再实现查询、类型筛选、关键词搜索、空状态和读取失败处理。
  - [x] SubTask 1.4: 执行后端定向测试。

- [x] Task 2: 接入工作台一级入口和只读页面
  - [x] SubTask 2.1: 按现有导航模式在编辑与剪藏之间增加“工作台”入口，并在 Electron 与浏览器模式保持可访问。
  - [x] SubTask 2.2: 新增或接入独立工作台页面，默认展示“全部内容”、项目兼容摘要、统一内容列表、类型筛选与关键词搜索。
  - [x] SubTask 2.3: 复用现有主题令牌和组件样式，实现加载、空数据、请求失败及重试状态，适配窄屏。
  - [x] SubTask 2.4: 执行前端脚本语法检查或项目现有前端校验。

- [x] Task 3: 完成端到端验证并同步规格状态
  - [x] SubTask 3.1: 验证内容列表覆盖剪藏、知识、待办和学习计划的可用索引项，且不展示正文。
  - [x] SubTask 3.2: 验证类型筛选、关键词搜索、项目兼容只读展示和后端失败重试。
  - [x] SubTask 3.3: 执行后端全量测试、可用的前端校验及桌面/浏览器冒烟测试。
  - [x] SubTask 3.4: 每项通过后勾选本文件与 `checklist.md` 的对应项目；发现需要产品取舍的问题时暂停并请求确认。

# Task Dependencies
- Task 2 依赖 Task 1 的数据契约。
- Task 3 依赖 Task 1 和 Task 2。

