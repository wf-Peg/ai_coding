# L2 工作台规则筛选与排除覆盖实施任务

- [x] Task 1: 定义规则与排除数据模型
  - [x] 添加 `WorkspaceRule` 和 `WorkspaceExclusion` 模型。
  - [x] 固定允许的规则字段和操作符。
  - [x] 定义规则、排除和解析统计结果的数据结构。

- [x] Task 2: 扩展本地 JSON 持久化
  - [x] 先编写并运行规则持久化失败测试。
  - [x] 新增 `workspace-rules.json` 和 `workspace-exclusions.json` 读写。
  - [x] 实现规则保存、更新、启用/禁用、读取和删除。
  - [x] 实现排除保存、幂等、读取、移除和工作台删除隔离。

- [x] Task 3: 实现规则解析与排除优先
  - [x] 先编写并运行规则解析失败测试。
  - [x] 实现 type/category/tag/sourcePath/workflowStatus/updatedAt 字段匹配。
  - [x] 实现 equals/contains/in/before/after 操作符。
  - [x] 实现规则并集、按 ContentRef.id 去重和禁用规则跳过。
  - [x] 合并手动成员和关系带入结果，并让排除记录优先级最高。
  - [x] 返回规则命中、手动加入、关系带入、排除和最终可见数量统计。

- [x] Task 4: 完成验证与文档收尾
  - [x] 运行 L2 定向测试、后端全量测试和编译。
  - [x] 验证不写入 WorkspaceMembership、不修改原始数据和旧 Project 文件。
  - [x] 更新本文件、验收清单和主线任务说明。
  - [x] 追加 commit_history.log，提交并推送远程分支。

- [x] Task 5: 将 L2 解析逻辑集成到 WorkspaceIndexService
  - [x] 新增 `resolveWorkspace(workspaceId, contentRefs, relationMembers)` 统一入口。
  - [x] 自动读取工作台和持久化手动成员，委托规则、排除和统计解析。
  - [x] 工作台不存在时明确失败，解析过程不写入永久成员关系。
  - [x] 补充集成测试并验证全量测试。

> 状态：已完成（2026-08-04）。后端全量测试与编译通过。

# Task Dependencies
- Task 2 依赖 Task 1。
- Task 3 依赖 Task 1 和 Task 2。
- Task 4 依赖 Task 1、Task 2 和 Task 3。
