# L2 规则与排除 Controller 验收清单

- [x] 规则列表、新增、更新、删除接口路径符合规格。
- [x] 排除列表、新增/幂等更新、删除接口路径符合规格。
- [x] 解析结果接口路径符合规格。
- [x] 请求不能覆盖 workspaceId、规则 id 和时间字段。
- [x] 合法规则可以持久化并返回服务端生成的字段。
- [x] 非法规则返回 400 和 `{status,message}`。
- [x] 不存在的工作台返回 404 和 `{status,message}`。
- [x] 索引损坏或读写失败返回 503 和 `{status,message}`。
- [x] 排除新增后，解析结果不再包含对应内容。
- [x] 排除重复新增保持幂等。
- [x] 解析结果包含内容元数据和来源统计，不包含 `content` 正文。
- [x] Controller 统一调用 WorkspaceIndexService，不复制规则解析逻辑。
- [x] 原有 `/api/workspace/overview` 行为保持兼容。
- [x] Controller 定向测试、后端全量测试和编译通过。


## 阶段结论（2026-08-05）
- 后端 L2 Controller 接口阶段验收通过。
- 前端工作台页面及接口交互尚未实现，不计入本阶段完成范围。
