# 需求分析：SQLite本地索引层M4（全库统一搜索）

## 原始需求
把本地全文检索从「仅 clip」扩展为「全库统一搜索」——跨 clip / knowledge / learning-plan 全部实体命中，返回统一类型化命中结果，前端可据此渲染统一搜索列表，进一步摆脱 Java 后端。

## 分析结论
M3 已把 knowledge / learning-plan 实体写入 `content` 表（与 clip 共用 FTS5 倒排），数据层已天然可检索全部实体，但存在缺口：

1. `search()` 只反序列化 `content_ref` 原样返回，未带类型标记。
2. `searchByCategory` 依赖 `category` 列，knowledge/learning-plan 一般无 category。
3. 无类型过滤能力、无统一命中视图。

本阶段新增类型感知的全库统一搜索 `searchAll`，返回统一命中结构，同时保留原 clip `search`/`searchByCategory` 兼容既有调用。

## 会话摘要
- 完成 M4.1：search.js 新增 `searchAll`（FTS + LIKE 双路径 + type 过滤），返回 `{type, id, title, snippet}` 统一结构。
- 完成 M4.2：main.js IPC `local-index:search-all` + preload 桥接 `localIndex.searchAll`。
- 完成 M4.3：apiClient.searchAll（本地优先，REST 兜底 `/api/local-search`）。
- 完成 M4.4：search.test.js 单测（跨实体命中/type 过滤/LIKE 兜底/统一结构/空守卫），4/4 通过；全量 SQLite 回归 19/19；editor 无回归。