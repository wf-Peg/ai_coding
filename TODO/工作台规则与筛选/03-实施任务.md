# 工作台规则与筛选 — 实施任务

## 一、后端模型层

### 1.1 规则表达式模型
- [x] 创建 `RuleGroup.java` — group record（id, relation, ruleIds）+ relation 归一化
- [x] 创建 `RuleExpression.java` — expression record（workspaceId, relation, groups）+ empty 工厂
- [x] 创建 `RuleExpressionUpdateRequest.java` — PUT 请求体

### 1.2 规则引擎升级
- [x] `WorkspaceRuleService.java`：表达式存储（readExpressions/writeExpressions 原子写）
- [x] `WorkspaceRuleService.java`：惰性迁移（getExpression 幂等）
- [x] `WorkspaceRuleService.java`：重写 resolve() 为逐条ref求值
- [x] `WorkspaceRuleService.java`：改造 saveRule/removeRule/deleteWorkspaceData 同步表达式
- [x] `WorkspaceRuleService.java`：新增 addGroup/deleteGroup 方法
- [x] `WorkspaceRuleService.java`：resolve() 构建反向成员索引（workspace字段支持）
- [x] `WorkspaceRuleService.java`：rawMatches() 新增 workspace 字段分支

### 1.3 Workspace 数据模型扩展
- [x] `Workspace.java`：新增 matchAll / isDefault / sortOrder 字段
- [x] `SuggestionCandidate.java`：新增 type / title / suggestedField / suggestedValue 字段

### 1.4 标题兜底
- [x] `ContentRefMapper.java`：新增 resolveTitle 辅助方法
- [x] 修改 fromClip/fromTodo/fromKnowledge/fromLearningPlan 使用 resolveTitle

## 二、后端控制器层

### 2.1 表达式端点
- [x] `WorkspaceController.java`：GET/PUT `/{workspaceId}/rule-expression`
- [x] `WorkspaceController.java`：POST `/{workspaceId}/rule-expression/groups`
- [x] `WorkspaceController.java`：DELETE `/{workspaceId}/rule-expression/groups/{groupId}`
- [x] `WorkspaceController.java`：RuleRequest 增加 groupId 字段

### 2.2 设置端点
- [x] `WorkspaceController.java`：PUT `/{workspaceId}/settings`（matchAll）
- [x] `WorkspaceController.java`：PUT `/{workspaceId}/set-default`
- [x] `WorkspaceController.java`：PUT `/workspace/reorder`

### 2.3 全局筛选端点
- [x] `WorkspaceController.java`：GET `/overview` 增加可选 workspaceId 参数
- [x] `WorkspaceController.java`：overview 中 workspaceId 有值时返回内容维度统计
- [x] `ClipController.java`：getClipList 增加 workspaceId 参数 + filterByWorkspace
- [x] `TodoController.java`：getTodoList 增加 workspaceId 参数
- [x] `KnowledgeController.java`：listKnowledge 增加 workspaceId 参数
- [x] `LearningPlanController.java`：getAllPlans 增加 workspaceId 参数

### 2.4 建议端点
- [x] `WorkspaceController.java`：增强 GET suggestions 合并规则建议
- [x] `WorkspaceController.java`：增强 PUT accept 区分规则/内容建议
- [x] `WorkspaceController.java`：新增 GET suggestion detail 端点

### 2.5 内容自动归属
- [x] `ClipRequest.java`：新增 workspaceId 字段
- [x] `TodoController.java` / `TodoRequest.java`：新增 workspaceId 支持
- [x] `ClipController.addClip()`：创建内容后自动创建成员关系（source=manual_input）
- [x] `TodoController.addTodo()`：同理创建成员关系

## 三、后端服务层

### 3.1 规则建议
- [x] `WorkspaceSuggestionService.java`：新增 generateRuleSuggestions()
- [x] `WorkspaceSuggestionService.java`：新增 acceptRuleSuggestion()

### 3.2 索引修复
- [x] `ProductDevWorkspaceInitializer.java`：注入 ContentIndexService
- [x] `ProductDevWorkspaceInitializer.java`：scanAndImport() 后立即重建索引
- [x] `ProductDevWorkspaceInitializer.java`：exists 分支改为仅在表达式缺失时自愈
- [x] `ScheduledTasks.java`：添加 TODO 目录定时扫描（每5分钟）

### 3.3 侧边栏
- [x] `WorkspaceIndexService.java`：新增 reorderWorkspaces() 方法

## 四、前端 workspace.html

### 4.1 规则区UI重构
- [x] 规则区 HTML 结构调整（添加分组按钮、根关系控件容器）
- [x] 新增 renderRootRelation() 根关系控件
- [x] renderExpression() 分组卡片增强（删除分组按钮）
- [x] 新增 addGroup() / deleteGroup() 函数
- [x] 新增 updateTabVisibility() 全部工作台隐藏Tab
- [x] CSS 样式（根关系、分组删除、按钮样式）

### 4.2 规则弹窗优化
- [x] 字段-操作符映射表
- [x] 动态值输入控件切换
- [x] Notion 风格时间选择器（日历面板）
- [x] 标签多选输入（胶囊样式、自动补全）
- [x] 操作反馈优化（loading、Toast）

### 4.3 AND 模式支持
- [x] renderWorkspaceSettings() 渲染 toggle 开关
- [x] 修复开关回弹 bug（PUT 响应刷新 currentExpression）

### 4.4 侧边栏改进
- [x] 默认工作台改为"全部"
- [x] 绿点标识渲染（default-indicator）
- [x] 右键菜单 showContextMenu()
- [x] 详情页设置默认按钮
- [x] 拖拽排序（drag 事件监听 + localStorage 持久化）

### 4.5 标题兜底
- [x] 4处 "未命名内容" → "无标题"

### 4.6 作用域筛选Bug修复
- [x] 删除 #scopeManageBtn
- [x] 新增 overviewRequestId 请求计数器
- [x] loadOverview() 校验请求过期

### 4.7 建议Tab优化
- [x] 详情弹窗 HTML+CSS
- [x] renderSuggestions() 检测规则建议卡片
- [x] openSuggestionDetail() / closeSuggestionDetail()
- [x] 操作 Toast 反馈

### 4.8 全局筛选
- [x] loadWorkspaces() 填充 overviewScopeSelect
- [x] 选择工作台时写入 localStorage
- [x] 启动时从 localStorage 恢复状态

### 4.9 统一详情页
- [x] detailTypeFilters 类型筛选按钮
- [x] renderDetailTypeFilters() 函数
- [x] 产品开发工作区 renderPdTagFilter 增加类型筛选

## 五、前端其他页面

### 5.1 index.html
- [x] activeWorkspaceId 全局状态
- [x] renderView('knowledge') 发送 workspaceChange 消息
- [x] storage 事件监听 + notifyAllFrames()

### 5.2 clip.html / todo.html / knowledge.js / learning-plan.html
- [x] 删除 workspaceBanner 横幅
- [x] 修改 workspaceChange 消息处理
- [x] 列表请求附加 workspaceId 参数

## 六、测试

### 6.1 后端测试
- [x] `WorkspaceRuleGroupTest.java` — 分组 CRUD 测试（5个用例）
- [x] `WorkspaceRuleServiceTest.java` — 表达式语义测试（嵌套分组、空表达式、惰性迁移）
- [x] `ProductDevWorkspaceRulesTest.java` — 内置表达式测试
- [x] `ContentRefMapperTest.java` — 标题兜底测试
- [x] `WorkspaceFilterTest.java` — workspaceId 筛选测试

### 6.2 构建验证
- [x] `mvn compile` — 编译通过
- [x] `mvn test` — 全量测试通过（已知预存失败除外）
- [x] `mvn package` — JAR 构建成功
- [x] `node --check workspace.html` — JS 语法通过