# 实施任务：画布对标幕布（大纲与画布）

> 对应设计方案：`.trae/documents/画布模块对标幕布-大纲与画布双向方案.md`
> 参考产品：幕布（https://mubu.com/home）——把「极简大纲 ↔ 思维导图」作为画布模块的最终产品方向。
> 范围铁律：不动 `index.html`、不动知识图谱模块、后端零逻辑改动（只补默认字段）。
> 分阶段：本批为 MVP，富文本/协同等复杂项留后续阶段。

---

## 功能点 fp-001：画布多文档 + 层级字段（数据层 schema v7）

- [x] `electron/sqlite/init.js`：新增 `canvas_doc(id, title, sort_index, created_at, updated_at)` 表
- [x] `canvas_node` 补列 `doc_id` / `parent_id` / `order_index`
- [x] `canvas_edge` / `canvas_group` 补列 `doc_id`
- [x] 建索引 `idx_canvas_node_doc(doc_id,parent_id,order_index)`、`idx_canvas_edge_doc`、`idx_canvas_group_doc`
- [x] 幂等补列工具 `hasColumn` / `addColumnIfMissing`（表名/列名白名单，杜绝拼接注入）
- [x] 回填 `backfillCanvasV7`：历史数据统一挂默认文档 `doc:default`；`order_index` 按 `created_at` 在文档内递增补号；`parent_id` 保持 NULL（视觉与升级前一致）
- [x] `SCHEMA_VERSION` 升到 7，且改为「逐级执行 + 最后写版本号」，避免跳级导致后续迁移被跳过

## 功能点 fp-002：本地索引层画布模块

- [x] 新建 `electron/sqlite/canvas-doc.js`：`listDocs/getDoc/createDoc/renameDoc/deleteDoc/ensureDefaultDoc/touch/clearAll`，常量 `DEFAULT_DOC_ID='doc:default'`、`DEFAULT_DOC_TITLE='我的画布'`
- [x] `canvas-node.js`：`listNodes(db, docId)` / `listEdges(db, docId)` 按文档过滤
- [x] `canvas-node.js`：`createNode` 支持 `docId/parentId/orderIndex`
- [x] `canvas-node.js`：新增 `saveStructure(db, entries)` 批量改父/改序，事务内**环检测**（禁止把祖先挂到后代下）
- [x] `canvas-node.js`：`descendantIds` + `deleteNode` 支持**整棵子树**删除
- [x] `canvas-node.js`：`docIdOfNode`（供边/分组推导所属文档）
- [x] `canvas-group.js`：`listGroups(db, docId)`、`createGroup` 带 `docId` 并 touch 文档更新时间

## 功能点 fp-003：快照 v2 与后端 docs 字段

- [x] `electron/canvas-sync.js`：`SNAPSHOT_VERSION` 升到 2，`buildSnapshot` 输出 `{docs,nodes,edges,layout,groups,updatedAt,version}`
- [x] `updatedAt` 取所有表（含 `canvas_doc.updated_at`）最大值
- [x] `wipe()` 刻意保留 `canvas_doc`（文档本身不随内容快照清空）
- [x] `restoreSnapshot` 支持 v2：恢复 `docs` + 节点 `doc_id/parent_id/order_index` + 边/分组按节点归属推导 `doc_id`
- [x] **v1 旧快照向后兼容**：无 `docs` 时自动归入 `doc:default`，并为节点补 `order_index` 序号
- [x] `CanvasStateService.java`：`DEFAULT_SNAPSHOT_VERSION` 升到 "2"；`readState` 补 `docs` 默认字段；`defaultSnapshot` 输出 `docs`
- [x] `CanvasController.java`：JSDoc 描述更新（画布文档/节点/连线/坐标/分组）——无逻辑变更

## 功能点 fp-004：IPC 接口与 preload 桥

- [x] `electron/main.js` 新增 `local-index:canvas:list-docs`（内部先 `ensureDefaultDoc`，保证首启即有默认文档）
- [x] `local-index:canvas:create-doc` / `rename-doc` / `delete-doc`
- [x] `local-index:canvas:state`：一次返回 `{doc, docs, nodes, edges, groups, layout}`，layout 仅保留当前文档内的 nodeId
- [x] `local-index:canvas:update-structure`：转调 `saveStructure`
- [x] `canvas:create-node` 扩展 `docId/parentId/orderIndex`；`canvas:list-groups`/`canvas:create-group` 支持 `docId`
- [x] `electron/preload.js` 暴露 `listCanvasDocs/createCanvasDoc/renameCanvasDoc/deleteCanvasDoc/canvasState/saveCanvasStructure`
- [x] 所有画布写操作后统一 `canvasSync.schedulePush()`（400ms 防抖）推快照

## 功能点 fp-005：视图三态与大纲面板骨架

- [x] 新建 `frontend/styles/canvas.css`：文档切换器 / 视图切换 / 大纲面板 / 文档列表全量样式，颜色全部走 `--app-*` 主题令牌
- [x] `frontend/canvas.html`：引入 `styles/canvas.css`
- [x] 顶栏：文档切换器（当前文档名 + 新建/重命名/打开列表）、视图三态切换（大纲 / 分栏 / 画布）、一键按大纲排版按钮、节点规模提示位
- [x] 内容区包 `.canvas-body[data-view-mode]`，新增 `<aside class="outline-panel">`（含展开/收起全部、导出 Markdown、`#outlineTree`、空态）
- [x] 新增文档列表遮罩 `#docListMask`（网格卡片 + 新建/关闭）
- [x] 引入 `<script src="js/canvas-outline.js">`（在 `canvas.js` 之前）
- [x] HTML 结构校验：`div` 47/47 平衡、`aside` 1/1

## 功能点 fp-006：大纲编辑器（幕布键盘范式）

- [x] 新建 `frontend/js/canvas-outline.js`，IIFE 暴露 `window.CanvasOutline`
- [x] 对外 API：`init/setNodes/upsertNode/removeNode/reset/highlight/focusFirst/setSelected/expandAll/collapseAll/toMarkdown/downloadMarkdown/getSelectedId/getCount/flush`
- [x] 可编辑契约：`onEdit/onStructure/onCreate/onDelete/onSelect/onScaleWarn/getExportName`
- [x] 键盘范式：`Enter` 插入同级、`Tab` 缩进为上一兄弟的末子、`Shift+Tab` 反缩进、`Alt+↑↓` 同级移动、`Ctrl/Cmd+↑↓` 折叠切换
- [x] 空行 `Backspace` 合并/反缩进；`Esc`、方向键在光标边界跳相邻行
- [x] 结构变更即时重排 + 同级 `orderIndex` 规范化，只把**变化了的行**合并成一次 IPC（`saveStructure`）
- [x] 文本编辑 300ms 防抖落库；行 DOM 按 id 复用，仅按序 `insertBefore`，不整体重建
- [x] 粘贴一律按纯文本插入（`onPaste`），避免外部 HTML 破坏行结构
- [x] Markdown 导出（`toMarkdown` / `downloadMarkdown`），按层级输出列表
- [x] 规模保护：节点数 > 500 自动折叠 + `onScaleWarn` 提示

## 功能点 fp-007：画布 ↔ 大纲双向联动 + 一键按大纲排版

- [x] `frontend/canvas.js` 数据源改走 `canvasState({docId})`，保留无桥兜底路径
- [x] 选中联动：画布点选 → 大纲高亮/滚动定位；大纲点选 → 画布节点高亮放大
- [x] 大纲编辑文本 → 局部刷新画布卡片文字（便签/引用卡局部更新，链接/图片卡退回整体重绘）
- [x] 大纲增删节点 → 复用 `createCanvasNode` / 子树删除，画布实时同步
- [x] 一键「按大纲排版」：结构 → `d3.hierarchy` + `d3.tree().nodeSize([84,170])` 分层坐标（左→右）→ 批量落库；无层级时按 `orderIndex` 竖排兜底
- [x] 排版后自动 `fitToContent` 适配视口
- [x] 视图模式持久化到 `localStorage`（`canvas_view_mode_v1`）；切视图后让 d3 重新量尺寸，保留当前缩放/平移
- [x] 大纲视图下跳过画布重绘（性能）

## 功能点 fp-008：多画布文档列表与切换

- [x] 文档切换器下拉：列出全部文档（含节点数）、切换、重命名、删除、打开文档列表、新建
- [x] 文档列表视图：卡片网格展示标题 + 节点数，支持打开/重命名/删除
- [x] 新建/重命名/删除均有自定义模态（复用现有弹层，补 `rename-doc`、`confirm-delete-doc`、`message` 三种模式）
- [x] 切换文档后重载数据与大綱、刷新文档名与规模提示
- [x] 导出 PNG 文件名带当前文档标题（`<文档标题>-画布-时间.png`）

## 功能点 fp-009：单测与主题合规

- [x] `electron/canvas-sync.test.js` 扩到 v2：快照组装、恢复一致、多文档隔离 + 层级字段保留、v1 旧快照归默认文档 + 补号、空快照安全恢复、异常快照容错
- [x] `node --test` 全绿（135 项，134 pass / 1 skipped / 0 fail）
- [x] `node --check` 全绿（main.js / preload.js / canvas-sync.js / canvas-doc.js / canvas-node.js / canvas-group.js / canvas.js / canvas-outline.js）
- [x] 主题合规：`node scripts/smoke-theme.js` 126/126 通过（canvas.css 未引入硬编码颜色）

---

## 后续阶段（先记录，本次不做）

- [ ] 富文本行内样式（加粗/高亮/链接）——对标幕布的样式能力
- [ ] 大纲行拖拽排序（鼠标拖动手柄）
- [ ] 大纲 ⇄ 画布位置反向同步（画布拖动后回写 hierarchy 相对次序）
- [ ] 多选批量缩进/移动、跨文档剪切粘贴
- [ ] Markdown / OPML 导入（把已有大纲文本直接建树）
- [ ] 搜索定位（大纲内全文检索跳行）
