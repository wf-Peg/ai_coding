# 实施任务：画布墨迹持久化（多文档隔离）

> 对应设计文档：`02-设计文档.md`；需求分析：`01-需求分析.md`
> 目标：墨迹不再是内存「临时擦写板」——落库、按文档隔离、纳入跨设备快照，刷新/重启/切换文档都不丢。
> 范围铁律：不动 `index.html`、不动知识图谱模块、后端零逻辑改动（仅补默认字段与版本号）。
> 分阶段：本批为 MVP；「墨迹 ↔ 节点关联、排版/转大纲带上墨迹」留后续阶段。

---

> **当前进度（2026-09-18 · 开发中，未提交）**
> 本批已完成：代码层 fp-001~fp-005 全部落地、`node --check` 通过；fp-006 单测与全量回归通过（166 项：160 pass / 5 fail / 1 skip，5 个失败均为既有无关项）；`INK_MAX_PER_DOC` 已对齐方案定为 800。
> 此项改动**保留在工作区、未提交**。
> 剩余：**fp-007 的浏览器实测与 04-验收清单逐条走查**。
> 涉及文件：`init.js`(v8) / 新建 `sqlite/canvas-ink.js` / `canvas-doc.js` / `canvas-sync.js`(快照 v3) / `main.js` / `preload.js` / `frontend/canvas.js` / `CanvasStateService.java`；新增 `sqlite/canvas-ink.test.js`，改 `db.test.js` / `canvas-sync.test.js`。

---

## 功能点 fp-001：数据层 schema v8 + 墨迹表模块　✅ 本批已完成

- [x] `electron/sqlite/init.js`：新增 `SQL_V8`，建 `canvas_ink(id, doc_id, color, width, opacity, points, sort_index, created_at, updated_at)`
- [x] `SQL_V8_INDEXES`：建 `idx_canvas_ink_doc(doc_id, sort_index)`
- [x] `SCHEMA_VERSION` 升到 8；在 `migrate()` 的 `if (current < SCHEMA_VERSION)` 块内追加 `if (current < 8) { db.exec(SQL_V8); db.exec(SQL_V8_INDEXES); }`
- [x] 确认新表不涉及补列，`addColumnIfMissing` 的 `TABLES/COLUMNS` 白名单无需扩展
- [x] 新建 `electron/sqlite/canvas-ink.js`：常量 `INK_MAX_PER_DOC`（默认 800）、`newInkId()`（`ink:` + base36 时间戳 + 随机后缀）
- [x] `listInk(db, docId)`：按 `sort_index, created_at` 升序返回，`points` 反序列化为数组
- [x] `saveInk(db, docId, strokes)`：**整文档全量替换**（事务内 `DELETE ... WHERE doc_id=?` + 批量 INSERT），返回 `{success, saved, truncated}`
- [x] `saveInk` 内做 points 合法性校验（数组、元素为长度 2 的有限数数组），非法笔迹跳过
- [x] `saveInk` 内超 `INK_MAX_PER_DOC` 截断最旧笔迹并置 `truncated`
- [x] `saveInk` 成功后调用 `canvasDoc.touch(db, docId)` 刷新文档时间戳
- [x] `clearDoc(db, docId)` / `clearAll(db)` / `countInk(db, docId)`
- [x] 导出模块常量与函数，风格对齐 `canvas-doc.js`

## 功能点 fp-002：文档级联与清空一致性　✅ 本批已完成

- [x] `electron/sqlite/canvas-doc.js` `deleteDoc`：事务内补 `DELETE FROM canvas_ink WHERE doc_id = ?`，并把条数计入 `removed.ink`
- [x] `deleteDoc` 的 `removed` 初始对象补 `ink: 0`
- [x] 复核 `clearAll`（文档层）与墨迹表无耦合，无需改动
- [x] 前端「清空全部内容」路径：清内存 `strokes` 的同时调用落库清空当前文档墨迹

## 功能点 fp-003：快照 v3 与后端字段　✅ 本批已完成（含 CanvasStateService.java 补 ink / 版本 3）

- [x] `electron/canvas-sync.js`：`SNAPSHOT_VERSION` 升到 3
- [x] `buildSnapshot`：新增 `ink`（`canvasInk.listInk` 全量，含 `docId`），并写入返回体
- [x] `buildSnapshot`：`updatedAt` 最大值计算**纳入 ink 的 `updated_at`**
- [x] `wipe()`：补 `canvasInk.clearAll(dbConn)`（杜绝幽灵墨迹）
- [x] `restoreSnapshot`：解析 `s.ink`；缺省（v2 快照）→ 空数组，保证 v2 兼容
- [x] `restoreSnapshot`：`docId` 不在快照 `docs` 内 → 归 `doc:default`；`points` 非法则跳过该笔
- [x] `restoreSnapshot`：事务内 `INSERT OR REPLACE INTO canvas_ink (...)`
- [x] `restoreSnapshot` 返回 counts 增加 `ink`
- [x] `backend/.../CanvasStateService.java`：`readState` 的 `putIfAbsent` 补 `ink`；`defaultSnapshot()` 补 `ink`
- [x] `CanvasStateService.java`：`DEFAULT_SNAPSHOT_VERSION` 提到 `"3"`；类 JSDoc 补 v3 说明（无逻辑变更）

## 功能点 fp-004：IPC 与 preload 桥　✅ 本批已完成

- [x] `electron/main.js`：require `./sqlite/canvas-ink`
- [x] `local-index:canvas:state`：返回体增加 `ink: localCanvasInk.listInk(dbConn, doc.id)`
- [x] `local-index:canvas:save-ink`：`{docId, strokes}` 校验（strokes 必须为数组、docId 必填）→ 转调 `saveInk`
- [x] `save-ink` 写成功且有变更时触发快照推送，但走 **2.5s 节流**（手绘高频，不复用节点编辑的 400ms `schedulePush`）
- [x] `electron/preload.js`：暴露 `saveCanvasInk({docId, strokes})` → `ipcRenderer.invoke('local-index:canvas:save-ink', {...})`

## 功能点 fp-005：前端载入与防抖落库　✅ 本批已完成（代码层；未浏览器实测）

- [x] `frontend/canvas.js`：`strokes` 元素补稳定 `id`（`handleDrawEnd` 生成），保留原有 `color/width/opacity/points`
- [x] `frontend/canvas.js`：新增 `inkDirty` / `inkTimer` 状态与 `markInkDirty()`（400ms 防抖 → `saveCanvasInk` → 成功后清 dirty）
- [x] `frontend/canvas.js`：新增 `flushInk()`（立即落库，供切文档/卸载前调用；无 dirty 时短路）
- [x] `frontend/canvas.js`：新增 `loadInk(incoming)`（库结构 → `strokes`；随后 `ensureInkLayer()` + `renderInk()`）
- [x] `frontend/canvas.js` `loadData()`：接入 `res.ink` 载入
- [x] 空文档擦写板专项：`loadData` 空文档分支当前是 `showEmpty + return`（不建 SVG/`ink-layer`）；改为「有墨迹时建 SVG + `ensureInkLayer()` + 隐藏空态遮罩，无墨迹才 `showEmpty`」，确保从零作画→刷新可还原
- [x] `frontend/canvas.js` `handleDrawEnd`：收笔后 `markInkDirty()`
- [x] `frontend/canvas.js` `eraseAt`：命中擦除后 `markInkDirty()`
- [x] `frontend/canvas.js` 撤销分支（`strokes.pop()` 处）：`markInkDirty()`
- [x] `frontend/canvas.js` 清空墨迹分支（`strokes = []` 处）：立即落库
- [x] `frontend/canvas.js` `switchDoc()`：首步 `flushInk()`，再清 `strokes`
- [x] `frontend/canvas.js` `clearCanvasAll`：清内存的同时落库清空当前文档墨迹
- [x] `frontend/canvas.js`：`beforeunload` + `visibilitychange(hidden)` 时 `flushInk()`，避免关窗丢最后一笔
- [x] 落库失败（bridge 缺失/返回 !success）时保留 dirty 并静默重试，不打断作画

## 功能点 fp-006：单测与回归　✅ 本批已完成

- [x] 新建 `electron/sqlite/canvas-ink.test.js`：`saveInk` 全量替换语义、按 doc 隔离、points 非法跳过、超上限截断、`clearDoc`/`clearAll`/`countInk`
- [x] `electron/sqlite/db.test.js`：补 schema v8 迁移用例（v7 → v8 增量建表 + 连续 `migrate()` 幂等）
- [x] `electron/canvas-sync.test.js`：补快照 v3 往返（ink 保留）；v2 快照（无 ink）恢复后 ink 为空且不报错；`wipe` 后墨迹不残留
- [x] 补用例：`deleteDoc` 后该文档墨迹被清（无残留）
- [x] `node --check` 全部改动文件
- [x] `node --test` 全量回归，确认无**新增**失败（166 项：160 pass / 5 fail / 1 skip；5 个失败均为既有无关项：`dist-electron/.../integrations/dsh/.../test.mjs`×2、`scripts/test-build.js` 及 `release.sh`/`download-jre.sh` bash 语法校验）

## 功能点 fp-007：文档与验收　🔄 部分完成（自动化全过 + 浏览器 mock 实测核心闭环全过；真机项待确认）

- [ ] 按 `04-验收清单.md` 逐条走查并实时勾选（浏览器 mock 已勾 11 项；真机项标注 ⚠️ 待应用内确认）
- [x] 明确记录范围外项（墨迹不进大纲、不进 Markdown、排版后不随节点平移），避免被误判为缺陷

### 实测发现（本轮浏览器 mock 实测）

- [x] **缺陷-1（文案）**：删除画布确认框文案「该画布下的全部**节点、连线与分组**都会一并删除」未提及**墨迹**——墨迹其实也会被级联清理（实测确认）。已修复：`frontend/canvas.js:1209` 改为「节点、连线、分组**与手绘墨迹**都会一并删除」。

---

## 后续阶段（先记录，本批不做）

- 墨迹与节点的归属/吸附模型（三种候选：吸附最近节点 / 独立 kind 节点 / 用户显式框选），及修正归属的 UI
- 「一键按大纲排版」时把有归属的墨迹按节点位移一起平移
- 层级大纲面板与 Markdown 导出中表达墨迹
- 墨迹富样式（更多颜色、线型、文字标注）与 Douglas–Peucker 抽稀优化
