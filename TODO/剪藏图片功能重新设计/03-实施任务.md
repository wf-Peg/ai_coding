# 剪藏图片功能重新设计 - 实施任务

> 按 M1→M4 顺序执行；每个里程碑完成后勾选并做验证。

## Step 0 — TODO 归档
- [x] 创建 `TODO/剪藏图片功能重新设计/` 四份文档（01-需求分析 / 02-设计文档 / 03-实施任务 / 04-验收清单）
- [x] 提交 git 并更新 commit_history.log

## M1 — 后端地基（1-2 天）

### M1.1 ImageUtils 重写
- [ ] `storeImage` 改为 uuid 命名，存 `{clip.storage.path}/images/{yyMM}/{uuid}.{ext}`
- [ ] magic-byte 校验（jpg/png/gif/webp），不再仅依赖扩展名
- [ ] 缩略图生成（ImageIO，256px，jpg/png）
- [ ] 移除旧 `/api/clip/image/{category}/{file}` 路径生成逻辑（由新端点替代）

### M1.2 ClipImageController 新增
- [ ] `POST /api/clip/image/upload`（multipart；magic-byte、10MB 上限；返回 path/url/size）
- [ ] `GET /api/clip/image/{yyMM}/{fileName}`（含 `?thumb=1`；文件名白名单 `^[\w.-]+$` + normalize + 限定根目录）
- [ ] `POST /api/clip/image/cleanup-orphans`（扫描 images/ 清理未被引用的文件）
- [ ] `POST /api/clip/migrate-images`（旧数据迁移：搬文件 + 重写 content 引用 + 回填 imagePaths）
- [ ] `GET /api/clip/file/{uuid}`（doc-ai 附件下载）

### M1.3 数据模型
- [ ] `ClipContent`：imagePaths 语义注释更新；新增 `attachmentPath`（doc-ai 源文件）
- [ ] `ClipRequest`：新增 `imagePaths`；`imageDataList` 标废弃（保留解析）

### M1.4 ClipService / ClipController
- [ ] `saveClip` 移除 base64 imageDataList 处理；校验 imagePaths 存在于 images 根目录，写入 content 引用与 imagePaths
- [ ] doc-ai 源文件改存 `documents/` + attachmentPath（不再走 imageUtils）
- [ ] `deleteClip`：删除该剪藏引用的图片（未被其他剪藏引用时）
- [ ] `updateClipFromEditor`：重扫 content 图片引用 reconcile imagePaths

### M1.5 ContentOrganizeService
- [ ] organize 复制引用图片到 `{organizedPath}/{category}/assets/{uuid}.{ext}`（扁平化）
- [ ] 修复引用重写（`./assets/{uuid}.{ext}`，原正则丢 category/子目录问题）

## M2 — 前端恢复（1 天）

- [ ] frontend/clip.html 恢复图片区（ai-text/store-only 显示）：
  - [ ] 粘贴事件 → canvas 压缩（1600px/q0.82/PNG 透明保留）
  - [ ] 上传按钮多选 → 压缩 → XHR multipart 上传（进度条、失败重试）
  - [ ] 成功后**光标处**插入 `![图片](images/...)`
  - [ ] 预览缩略图 + 删除（同时移除内容中引用）
- [ ] 提交剪藏携带 `imagePaths`
- [ ] marked renderer 图片 URL 重写（`rewriteImageSrc`，按 API origin）

## M3 — 扩展对齐（1 天）

- [ ] clip-main.js：替换 Phase 3 base64 实现为新上传流（压缩/进度/光标插入/预览删除）
- [ ] 扩展 clip.html renderer 同步
- [ ] 前端与扩展行为一致性核对

## M4 — 迁移与验收（0.5 天）

- [ ] `migrate-images` 跑通存量数据（真实 3 张图）
- [ ] 冒烟测试（详见 04-验收清单.md）
- [ ] `mvn -q -DskipTests compile` / `test-compile` 通过；JS 语法检查通过
- [ ] 提交 git 并更新 commit_history.log、归档验收结果

## 决策点（默认倾向，如无异议按默认执行）

| ID | 决策 | 默认 |
|---|---|---|
| D-A | 上传时机：粘贴即传（非随提交） | 即传 |
| D-B | content 引用：相对路径 + 渲染重写 | 相对路径 |
| D-C | 压缩：客户端 canvas 为主 + 后端 ImageIO 缩略图 | 客户端为主 |
| D-D | 旧数据：一次性迁移接口 | 迁移接口 |
| D-E | 图片目录：`clip-storage/images/` | 同根 |
| D-F | doc-ai 附件：独立 `documents/` | 独立 |
| D-G | 列表缩略图：M1 生成 256px | 做 |
