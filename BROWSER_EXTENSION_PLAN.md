# 浏览器插件方案与重构建议

## 1. 结论

这个项目已经有浏览器插件雏形，不建议从零重做，建议走“保留现有插件目录，先做成稳定的采集入口，再逐步做智能增强”这条路。

当前已有基础：

- 插件清单和权限：`browser-extension/manifest.json`
- 右键菜单、快捷键、发送后端：`browser-extension/background.js`
- 页面内容提取：`browser-extension/content.js`
- 弹窗编辑：`browser-extension/popup.js`
- 后端剪藏入口：`backend/src/main/java/com/example/clip/controller/ClipController.java`

## 2. 对插件的产品定位建议

不要把插件做成“另一个前端页面”。

更合理的定位是：

- 浏览器侧高速采集器
- 主应用的轻入口
- 后端 AI 能力的前置采集层

插件核心目标建议只保留三个：

- 快速采集
- 轻量确认
- 无感入库

## 3. 最值得做的功能方向

### 3.1 高频核心功能

1. 一键剪藏当前页面
2. 选中文本后快速剪藏
3. 自动带上来源 URL、标题、选中文本上下文
4. 剪藏后立即返回工作流，不强迫用户填写复杂表单
5. 支持“稍后整理”，把 AI 分类、标签、摘要放到后端处理

### 3.2 更适合这个项目的增强功能

1. 剪藏后异步 AI 整理
2. 与日报/周报联动
3. 以“知识沉淀”而不是“收藏链接”为目标
4. 一键转待办
5. 高亮片段连同上下文一起保存

## 4. 推荐的插件产品形态

### 4.1 Quick Save

- 右键或快捷键后直接入库
- 默认只弹极小确认框
- 允许快速修改分类和标签
- 面向最高频使用场景

### 4.2 Save With Context

除正文外，同时保存：

- 页面标题
- URL
- 用户选中内容
- 页面摘要
- 抓取时间
- 站点名

这能明显提升后端搜索与 AI 整理质量。

### 4.3 Read Later / Inbox

- 插件采集内容统一先进入 `inbox`
- 后端负责后续分类、标签、整理、周报

这样插件侧体验更轻，主应用侧价值更强。

### 4.4 Highlight Mode

- 保存选中文本
- 同时抓取前后几段上下文
- 适合知识管理，而不是简单收藏

### 4.5 Clip To Todo

- 选中网页内容后直接转成待办
- 对当前项目的待办功能是天然补强

## 5. 现有实现的优先问题

### 5.1 环境耦合过死

`browser-extension/popup.js` 中“查看剪藏列表”直接写死到了 `http://localhost:3000/clip.html`。

问题：

- 对 Electron 打包版不友好
- 对后续部署不友好
- 无法兼容不同运行模式

### 5.2 内容提取逻辑重复

`browser-extension/background.js` 和 `browser-extension/content.js` 各维护了一套页面正文提取逻辑。

问题：

- 维护成本高
- 行为容易不一致
- 很难持续优化规则

### 5.3 文档与实现不完全一致

当前看到的差距：

- 图片剪藏目前只是保存图片 URL，不是真正上传图片
- 悬浮按钮发送 `clipCurrentPage`，背景脚本没有对应处理
- “插件端直接调用第三方大模型 API”与后端 AI 能力重复

### 5.4 安全边界不清晰

在插件本地保存第三方模型 API Key 风险偏高。

更合理的边界：

- 插件只调用你的后端
- 后端统一处理 AI 与密钥

### 5.5 扩展来源兼容性需要实测

后端 `ClipController` 当前允许本地网页来源与 `null`。浏览器扩展发起请求时，是否在所有目标浏览器中稳定通过，建议单独验证。

## 6. 架构建议

建议采用“轻插件 + 强后端 + 主应用承载管理”的结构。

### 6.1 插件职责

- 采集内容
- 提取上下文
- 轻量编辑
- 发送到后端
- 提供极简反馈

### 6.2 后端职责

- 内容入库
- 分类与标签生成
- AI 清洗与摘要
- 搜索索引
- 日报/周报整理
- 与 Git 同步能力协同

### 6.3 主应用职责

- 查看与编辑
- 搜索与筛选
- 二次整理
- 复盘与输出

## 7. 浏览器插件 MVP 需求清单

这一版目标不是“功能全”，而是“高频动作稳定可用”。

### 7.1 MVP 目标

- 让用户在浏览网页时，最低成本把信息收集进系统
- 采集链路稳定
- 减少用户在插件里停留的时间

### 7.2 MVP 功能范围

#### 必做功能

1. 剪藏当前页面
2. 剪藏选中内容
3. 自动保存标题、URL、抓取时间
4. 弹窗快速确认后发送后端
5. 支持默认分类为 `inbox`
6. 支持快捷键
7. 支持右键菜单
8. 发送成功或失败反馈

#### 建议纳入 MVP 的增强项

1. 选中文本附带前后上下文
2. 自动识别站点名
3. 打开主应用“今日收集箱”
4. 一键转待办

#### 不建议放入 MVP 的功能

1. 插件端直连第三方大模型
2. 图片真正上传
3. 页面截图
4. 多后端切换
5. 复杂规则市场

这些可以在第二阶段再做。

### 7.3 MVP 数据字段建议

建议插件发送的数据至少包含：

```json
{
  "type": "ai-text",
  "content": "正文或选中文本",
  "source": "https://example.com/article",
  "title": "页面标题",
  "category": "inbox",
  "tags": [],
  "useAiTags": true,
  "metadata": {
    "siteName": "example",
    "selectedText": "用户选中的原文",
    "contextBefore": "前文",
    "contextAfter": "后文",
    "capturedAt": "2026-04-26T12:00:00+08:00"
  }
}
```

如果当前后端暂时不支持 `metadata`，可以先兼容式扩展，或将上下文拼接进 `content` 中的结构化文本。

### 7.4 MVP 成功标准

- 当前页面和选中文本两条链路都可稳定入库
- 平均操作不超过 2 次点击
- 失败时能明确提示
- 用户能在主应用中快速看到刚刚保存的内容

## 8. MVP 交互流程

### 8.1 流程 A：剪藏当前页面

1. 用户点击快捷键或右键“剪藏当前页面”
2. 插件提取正文、标题、URL
3. 插件展示一个轻量确认弹窗
4. 用户可修改分类或补充标签
5. 点击提交
6. 插件发送到后端
7. 显示成功提示
8. 用户可选择“打开主应用查看”

### 8.2 流程 B：剪藏选中内容

1. 用户先在页面选择一段文字
2. 右键“剪藏选中内容”或快捷键触发
3. 插件获取选中文本与上下文
4. 弹窗预填内容
5. 用户快速确认
6. 发送后端并提示结果

### 8.3 流程 C：快速无弹窗剪藏

1. 用户点击“快速剪藏”
2. 插件使用默认配置直接入库
3. 成功后显示通知
4. 若失败，用户可点击“重新编辑”

这个流程适合高频场景，是后续很值得补的能力。

### 8.4 流程 D：一键转待办

1. 用户选择网页中的行动项
2. 触发“剪藏为待办”
3. 插件把选中文本作为待办标题或待办描述
4. 后端写入 todo
5. 主应用待办列表可直接查看

## 9. 基于现有 `browser-extension/` 的重构方案

目标不是全面推翻，而是沿现有目录结构做一次职责收敛和能力补强。

### 9.1 重构原则

1. 尽量保留现有目录结构
2. 不重复维护内容提取逻辑
3. 插件只做采集，不做重 AI 逻辑
4. 配置项尽量少而稳定
5. 所有主流程都要有失败提示和回退路径

### 9.2 文件级改造建议

#### `browser-extension/manifest.json`

建议调整：

- 明确最小权限集
- 若不再需要插件直连 OpenAI / Anthropic，可去掉相关 `host_permissions`
- 评估是否需要加入 `notifications`
- 保持 MV3 架构不变

#### `browser-extension/background.js`

建议重构为以下职责：

- 只处理入口事件
- 调度右键菜单、快捷键、popup 消息
- 调用统一采集方法
- 调用统一发送后端方法
- 管理通知和错误处理

建议拆出或抽象：

- `collectCurrentPage(tab)`
- `collectSelection(tab)`
- `sendClip(payload)`
- `notify(result)`

不建议继续保留：

- 直接内嵌另一套 `extractPageContent`
- 直接调用第三方大模型 API

#### `browser-extension/content.js`

建议成为唯一的页面提取入口：

- 提取正文
- 提取选中文本
- 提取上下文
- 提取标题、URL、站点名
- 统一清洗文本

建议新增返回结构：

```js
{
  content: "...",
  selection: "...",
  contextBefore: "...",
  contextAfter: "...",
  title: document.title,
  url: location.href,
  siteName: location.hostname
}
```

#### `browser-extension/popup.js`

建议聚焦为“轻确认弹窗”：

- 预填内容
- 修改分类
- 修改标签
- 提交
- 查看结果

建议补强：

- 读取默认配置并真正应用到表单
- 提供 `快速提交` 和 `提交后打开主应用`
- 不写死主应用地址，改为配置项

#### `browser-extension/options.js`

建议收缩配置项，避免过度设计。

建议保留：

- 后端 API 地址
- 主应用地址
- 默认分类
- 默认剪藏类型
- 是否启用通知
- 内容提取规则

建议移除或降级：

- 插件端大模型配置
- 第三方模型 API Key 存储

#### `browser-extension/popup.html` 与 `options.html`

建议界面目标：

- 减字段
- 提高确认效率
- 把高级配置收进 options
- popup 保持快速、少干扰

### 9.3 建议新增的模块拆分

如果你准备继续维护插件，建议增加几个公共文件：

- `browser-extension/lib/config.js`
- `browser-extension/lib/collector.js`
- `browser-extension/lib/api.js`
- `browser-extension/lib/constants.js`

这样可以把当前分散在 `background.js`、`content.js`、`popup.js` 的共用逻辑收敛起来。

### 9.4 建议的重构阶段

#### 第一阶段：稳定主链路

1. 统一内容提取逻辑
2. 统一发送后端逻辑
3. 去掉插件端第三方 AI 直连
4. 让 popup、右键、快捷键都走同一条主链路

#### 第二阶段：提升体验

1. 加入 `inbox`
2. 保存选中上下文
3. 打开主应用查看刚保存的内容
4. 支持快速无弹窗剪藏

#### 第三阶段：差异化增强

1. 一键转待办
2. 页面截图
3. 图片真实上传
4. 针对特定站点做提取优化

## 10. 我建议优先保留的 4 个入口

如果要控制复杂度，我建议插件先只保留这 4 个入口：

1. 剪藏当前页面
2. 剪藏选中内容
3. 剪藏为待办
4. 打开今日收集箱

这四个入口已经足够支撑一个高频、实用、符合当前项目定位的浏览器插件。

## 11. 实施优先级建议

### P0

- 修复并稳定插件到后端通信
- 统一提取逻辑
- 让右键、快捷键、popup 三条链路稳定
- 移除插件侧第三方 AI 直连

### P1

- 增加 `inbox`
- 自动保存标题、URL、站点名、上下文
- 支持剪藏后打开主应用

### P2

- 高亮批注
- 一键转 todo
- 页面截图剪藏
- 特定站点定制提取规则

## 12. 最终判断

这个方向值得做，而且你已经有明显基础。真正需要避免的是把插件做成“功能很多但不稳定的重前端”。

更适合当前项目的路线是：

- 轻插件
- 强后端
- 主应用负责管理和复盘

这条路线和你现在的仓库结构是匹配的，也更容易尽快做出可用成果。
