# 合并回归总恢复方案：全面对账 + 逐块回填

> 目标：停止「打地鼠」。先产出**完整被覆盖清单**，再按类别逐块回填本地主干(39aba92)丢失的功能到当前文件；同时**保留合并后合法提交**（GPU 修复等）。DSH 皮肤属运行时问题，单独走重装流程。

***

## 一、根因（为什么反复发现遗漏）

提交图显示 `c05301b`（Merge remote main into local main）把**远端 ed81494 重构线(39aba92 的另一条分叉)合入本地主干。合并对该次涉及的**数十个文件采用了远端版本，导致只在本地主干(39aba92)存在、远端线没有/被删的功能被整块覆盖。

- 上一轮已做的是**按用户症状逐条回填**，只覆盖了前端 UI + 后端牛马存储 + 打包脚本 3 类；而 `electron/main.js`、`preload.js`、`clip-capture/index.mjs`、后端 `WorkspaceController` 等**重灾区仍未对账**，且混入合并后的合法提交（GPU 修复），无法简单整体回退。 → 这正是你看到「还有很多没恢复」「越改越乱」的根源。

**证据（已核实）：**

| 功能                                                           | 本地(39aba92)有                            | 当前有                                            | 判定                 |
| ------------------------------------------------------------ | --------------------------------------- | ---------------------------------------------- | ------------------ |
| DSH 版本检测 `detectDshVersion` / `dsh-agent:detect-version` IPC | main.js\@1206/1234/1283、preload.js\@549 | ❌ 全无                                           | **被覆盖，需回填**        |
| 牛马自动归档 `clip-capture`（turn/end→聚合→POST）                      | index.mjs 完整(200+行)                     | ❌ 被删成极简版(\~60行)                                | **被覆盖，需回填**        |
| 后端 `/feature-points/iterations/ai-session` + AI 四字段提炼        | WorkspaceController.java\@1140/1146     | ❌ 无                                            | **被覆盖，需回填**        |
| DSH 界面皮肤/主题(web-ui 皮肤)                                       | 依赖运行时 bundle                            | 启动报 `cannot resolve @linxin666/dsh-web-ui-all` | **运行时问题，非 git 恢复** |

***

## 二、当前状态分析

### 已恢复（本轮前已完成，无需重复）

- 前端：工作台牛马卡片、设置页 DSH 升级助手 UI、后端异步解耦、知识详情 Vault 双链、主框架 openFileInEditor、编辑器自动换行、`AI干活→牛马`、深色主题（知识/学习计划/Wiki/剪藏更多功能）

- 后端：`FeaturePointIterationService.add()` 四字段+source

- 打包：`clean-artifacts.js`(保留 win-unpacked)、`prebuild-clean.js`(DSH 依赖校验)

### 仍缺失（本次方案回填对象）

1. **ELECTRON 主进程/预加载**：`electron/main.js`、`electron/preload.js` — DSH 版本检测链路（及同批被覆盖的其它功能块）
2. **牛马自动归档链**：`integrations/dsh/plugins/clip-capture/index.mjs` + 后端 `WorkspaceController` 的 `ai-session` 端点（含 AI 提炼）
3. **待核实的重灾文件**：electron sqlite（`indexer`/`scanner`/`index-service`/`db`）、`electron/logger`、浏览器扩展、`scripts/check-win-artifacts.js`、`electron/afterPack.test.js` — 需在三方对账中逐一分类
4. **DSH 运行时皮肤**（非代码）：`@linxin666/dsh-web-ui-all` 缺失

***

## 三、拟定改动（逐块回填，保留合法新提交）

### 第 1 步：生成完整被覆盖清单（一次性）

- 写临时 `scripts/dev/clobber-audit.mjs`（提交后删除）：

  - 遍历 `git diff --name-only 39aba92 c9f2c36`

  - 对每个文件比较 `git show c05301b:<f>` 是否等于 `c05301b^2:<f>`（即合并取远端）

  - 输出三态：`已恢复`（工作区≈39aba92）/ `合法重构保留`（远端合理改进）/ `需回填`

- 产出 `docs/合并回归覆盖清单.md` 作为单一事实来源，杜绝再漏。

### 第 2 步：回填 ELECTRON 版本检测（修复「设置-DSH升级助手 当前版本：—」）

- `electron/main.js`：从 39aba92 回填

  - `detectDshVersion(bin, config)`（含 npx 缓存扫描、按版本选最高、跳 `_npx` 残留路径）

  - 复用运行实例的 HTTP `/version` 探测（1.5s 超时）+ 磁盘兜底（`resolveDshBin`+`detectDshVersion`）

  - `ipcMain.handle('dsh-agent:detect-version')` 及其它被覆盖的同批 DSH IPC

  - 用 `git diff 39aba92..HEAD electron/main.js` 定位所有被删功能块，**手工合入当前 main.js**（保留 GPU 修复段）

- `electron/preload.js`：回填 `detectDshVersionState: () => ipcRenderer.invoke('dsh-agent:detect-version')`

### 第 3 步：回填牛马自动归档链（修复「牛马没记录」）

- `integrations/dsh/plugins/clip-capture/index.mjs`：以 39aba92 版为基，回填

  - 显式 `clip_session` 工具四字段

  - 自动路径：`session/event` 的 `turn/end`(reason=completed) 监听 → `aggregateTurn` 聚合 → 有产出信号(调用工具或输出≥threshold)且未显式归档时 POST `/api/workspace/feature-points/iterations/ai-session`

  - `OPERATIONAL_TOOLS` 过滤（纯运维工具不触发）

- 后端 `WorkspaceController.java`：回填 `POST /feature-points/iterations/ai-session` + 调 AI 提炼四字段落库（`source=dsh-session`）

- 已有 `FeaturePointIterationService.add()` 字段支撑，无需再改

### 第 4 步：DSH 运行时皮肤（非代码，重装）

- 核对 `node_modules/@deepseek-ai/dsh` 是否含 `@linxin666/dsh-web-ui-all`；缺失则执行（已记录的手动补装）：

  ```
  node "node_modules/@deepseek-ai/dsh/lib/bin.js" plugin --profile web install
  ```

- 重启 DSH 验证皮肤/主题恢复

- （此步是运维动作，纳入方案执行清单但不改仓库代码）

### 第 5 步：三方对账后补尾

- 对第 1 步清单中「需回填」的其余文件（electron sqlite、logger、browser-extension、check-win-artifacts.js、afterPack.test.js）按「功能有否在记忆硬约束/功能清单中」判定：有→回填；纯重构→保留。**逐一记录结论到清单**，不允许留「未核对」。

***

## 四、假设与决策

- **以本地主干(39aba92)为功能事实源**；`c9f2c36`(ed81494)仅当确认为「合理重构/新功能」才保留。

- **不整体回退 main.js/preload**：逐块手工合并，保留 GPU/退出秒退等合并后合法改进（它们的提交 92f508b/c08d05c/baa8899 位于合并之后，不能被覆盖）。

- 皮肤问题**不写进 git 恢复**，走运行时重装；如重装后仍不行，则单独排查 DSH 版本与 profile，不在本方案扩大范围。

- 方案执行期间**不引入新功能**，只恢复被覆盖的已有行为。

***

## 五、验证

1. `node --check electron/main.js electron/preload.js scripts/*.js` 及回填的源码语法。
2. `mvn -q -o compile` 后端编译通过。
3. 启动应用（先杀残留、清 lockfile）：

   - 设置-「DSH Agent/牛马」升级助手显示**真实版本号**（非 —），npx/内置/运行实例来源标注正确。

   - DSH 启动后**界面皮肤/主题正常加载**（@linxin666 bundle 补装生效）。

   - 跑一轮 DSH 会话 → 工作台「牛马记录」自动出现卡片（source=dsh-session），四字段齐全。
4. 回归：剪藏/知识/设置/Wiki 深色主题正常；工具页「牛马」入口正常。
5. `git status` 仅包含本次回填文件；无新增未说明改动。

