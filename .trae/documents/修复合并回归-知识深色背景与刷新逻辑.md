# 修复「合并/重构」引入的知识模块深色主题与刷新逻辑回归

## 摘要 Summary

用户反馈「深色主题下知识模块搜索行背景为灰白、不协调」的问题在之前会话修复后又复现。经追溯 git 历史与代码，确认根因是近期重构/合并提交 **`ed81494`（feat(frontend): 编辑器快捷键、设置与页面交互重构，2026-09-03 00:17）** —— 该提交虽是单父普通提交，但来源分支版本较旧，实际等同于把旧代码覆盖了回来，回退了上一会话对 knowledge 页的两处修复。同时顺带排查到同类的 `.header` 硬编码浅色背景还存在于 learning-plan 与 wiki 页面。

本次修复范围（用户已确认）：**知识模块背景 + knowledge 刷新逻辑**，并按用户追加要求**连带排查并修复其它页面的同类问题**。

## 现状分析 Current State Analysis

### 根因定位

1. **深色主题灰白背景回归**：`frontend/knowledge.html` 第 34-43 行的 `.header` 规则中同时存在两个 `background`：

   * 第 35 行 `background: var(--surface);`（正确的主题变量，上一会话的修复）；

   * 第 42 行 `background: rgba(255,255,255,0.85);`（`ed81494` 重新插入的硬编码值）。
     CSS 同一规则内**后声明的属性覆盖先声明**，第 42 行生效 → 深色主题下显示灰白。git blame 确认第 42 行新增来源即为 `ed81494`。

2. **刷新逻辑回归（双重刷新）**：`ed81494` 同时回退了上一会话「refresh → 轻量数据刷新（保留搜索词）」的修复：

   * `frontend/knowledge.js` 第 168-169 行 `refresh` 分支被改回 `location.reload()`（上一会话应为 `fetchTopics()`）；

   * `frontend/knowledge.html` 第 307-311 行**新增**了一段内联 `location.reload()` 的 refresh 监听。
     结果：主框架广播 `refresh` 时，knowledge.js 与内联脚本先后各触发一次整页重载，造成重复刷新（即用户之前抱怨的「重复刷新几次」）。

### 同类问题排查（用户追加要求）

扫描 `frontend/**/*.html`、`frontend/styles/**/*.css` 中所有硬编码浅色背景，结合上下文确认**真正同类的深色主题回归**仅以下三处 `.header`：

| 文件                            | 行  | 现象                                                      | 来源               |
| ----------------------------- | -- | ------------------------------------------------------- | ---------------- |
| `frontend/knowledge.html`     | 42 | `.header` 双 background，硬编码值后写覆盖 `var(--surface)`        | `ed81494`（本次回归）  |
| `frontend/learning-plan.html` | 79 | `.header` 双 background（72 行 `var(--surface)` + 79 行硬编码） | `4f0ec55`（历史遗留）  |
| `frontend/wiki.html`          | 54 | `.header` 仅硬编码 `rgba(255,255,255,0.9)`                  | `5707a1c5`（历史遗留） |

其余硬编码浅色背景均为**合理的非回归**，已排除：

* `settings.html:278`、`todo.html:864` = 开关滑块的小圆点（knob，白点正确）；

* `todo.html:329` = 复选框勾选标记（白勾正确）；

* `clip.html:213` = 强调色按钮上的白字白底（正确）；

* `workspace.css` toggle/klock、`clip.css`/`editor.css` 覆盖层高亮 = 在彩色/强调背景上使用白色，属设计意图。

## 拟改动 Proposed Changes

### 1. `frontend/knowledge.html`（报告的问题，必改）

* **第 42 行**：删除 `background: rgba(255,255,255,0.85);`，使 `.header` 仅保留第 35 行 `background: var(--surface);`，恢复深色主题贴合。

* **第 307-311 行**：删除内联 `location.reload()` 的 refresh 监听脚本块（`refresh` 由 `knowledge.js` 统一处理，避免双重重载）。

### 2. `frontend/knowledge.js`（chosen 范围内，刷新逻辑回滚）

* **第 168-169 行**：将 `refresh` 分支由 `location.reload()` 改为轻量数据刷新并保留搜索词：

  ```js
  } else if (e.data.action === 'refresh') {
    fetchTopics(document.getElementById('searchInput')?.value || '');
  }
  ```

  （`fetchTopics` 定义于第 9 行；`searchInput` 为搜索框 id，参照第 138/146 行既有用法。）

### 3. `frontend/learning-plan.html`（同类问题连带修复）

* **第 79 行**：删除 `background: rgba(255,255,255,0.85);`，保留第 72 行 `background: var(--surface);`。（与 knowledge 同模式）

### 4. `frontend/wiki.html`（同类问题连带修复）

* **第 54 行**：将 `background: rgba(255,255,255,0.9);` 改为 `background: var(--surface);`（与 knowledge/learning 的 `.header` 一致，保证深色主题贴合）。

### 5. （仅记录，本次不处理）`settings` 刷新回归

* `frontend/settings.html:1460-1465` 内联 `location.reload()`、`frontend/js/settings.js:1874-1875` refresh → `location.reload()`（上一会话为 `loadConfig()`）。按用户选择「背景+knowledge刷新」范围，**本次不改**，留作后续（若接入需一并确认 `hardRefresh`/滚动保留是否仍需要）。

## 假设与决策 Assumptions & Decisions

* **范围**：按用户选择修复知识背景 + knowledge 刷新；同时按追加要求连带修复 learning-plan、wiki 的同款 `.header` 硬编码背景。

* **`.header`** **语义**：统一以 `var(--surface)`（现有主题令牌，knowledge/learning 已使用）作为页面头部背景，替代硬编码浅色。

* **knowledge 刷新语义**：恢复「data-level refresh 保留搜索词」，避免整页重载与重复刷新；这符合上一会话修复意图与用户此前对重复刷新的反馈。

* **不改 settings 刷新**：超出本次选择范围，仅记录。

* **不动其它**：开关 knob、复选框勾选、强调色按钮等硬编码白色均判定为设计意图，不做改动，避免过度工程。

## 验证 Verification

1. `node --check frontend/knowledge.js` 语法通过。
2. 启动应用，将外观切到\*\*深色(dark)\*\*主题，进入知识模块，确认顶部搜索所在 `.header` 背景与页面背景协调（不再灰白）。核对学习计划、Wiki 两页头部同样贴合。
3. 启动后端触发 `refresh`（或点顶部刷新按钮），确认知识模块**只刷新一次**、且搜索框关键词保持，不再重复整页重载。
4. 快速回归：切回 default(light)/notion 主题，确认知识页头部背景正常。
5. `git diff` 检查改动仅限上述目标行，无多余变更；按项目约定更新 `commit_history.log`。

