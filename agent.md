# 剪藏项目开发实践指南

## 1. 开发环境搭建

### 1.1 后端开发环境
- **JDK**：Java 17
- **Maven**：3.8+ 
- **IDE**：IntelliJ IDEA 或 Eclipse
- **Spring Boot**：3.2.0

### 1.2 前端开发环境
- **Node.js**：16+ 
- **npm**：8+ 
- **IDE**：VS Code
- **React**：18+ 
- **Vite**：5+

### 1.3 环境配置
1. **后端配置**：
   - 复制 `application_templete.yml` 为 `application.yml`
   - 配置 DashScope API Key
   - 配置邮件服务（可选）

2. **前端配置**：
   - 运行 `npm install` 安装依赖
   - 运行 `npm run dev` 启动开发服务器

## 2. 编码规范

### 2.1 Java编码规范（参考阿里巴巴Java开发手册）

#### 2.1.1 命名规范
- **类名**：使用大驼峰命名法，如 `ClipController`
- **方法名**：使用小驼峰命名法，如 `saveClip`
- **变量名**：使用小驼峰命名法，如 `clipService`
- **常量名**：使用全大写加下划线，如 `MAX_TAGS`
- **包名**：使用小写字母，如 `com.example.clip`

#### 2.1.2 注释规范
- **类注释**：每个类都必须有类级注释，描述类的功能
- **方法注释**：每个方法都必须有方法级注释，描述方法的功能、参数和返回值
- **属性注释**：每个重要属性都应该有注释，描述属性的用途
- **代码块注释**：复杂的代码块应该有注释，解释代码的逻辑

#### 2.1.3 代码风格
- **缩进**：使用4个空格进行缩进
- **换行**：每行代码长度不超过120个字符
- **括号**：左括号与行尾相连，右括号与行首对齐
- **空格**：运算符两边、逗号后、分号后都应该有空格

### 2.2 前端编码规范

#### 2.2.1 命名规范
- **组件名**：使用大驼峰命名法，如 `ClipList`
- **变量名**：使用小驼峰命名法，如 `clipContent`
- **常量名**：使用全大写加下划线，如 `API_URL`
- **函数名**：使用小驼峰命名法，如 `handleAddClip`

#### 2.2.2 代码风格
- **缩进**：使用2个空格进行缩进
- **换行**：每行代码长度不超过100个字符
- **括号**：左括号与行尾相连，右括号与行首对齐
- **空格**：运算符两边、逗号后、分号后都应该有空格

## 3. 开发流程

### 3.1 后端开发流程
1. **需求分析**：理解需求，确定功能点
2. **设计**：设计类结构、方法签名和数据模型
3. **编码**：按照编码规范实现功能
4. **测试**：编写单元测试，确保功能正常
5. **集成**：与前端集成，测试API调用

### 3.2 前端开发流程
1. **需求分析**：理解需求，确定页面功能
2. **设计**：设计页面布局、组件结构和交互流程
3. **编码**：按照编码规范实现组件
4. **测试**：测试页面功能和响应式布局
5. **集成**：与后端集成，测试API调用

## 4. 核心功能实现指南

### 4.1 剪藏添加功能
- **后端实现**：
  - 在 `ClipController` 中实现 `addClip` 方法
  - 调用 `ClipService.saveClip` 保存剪藏内容
  - 根据类型处理不同的内容（文本、链接、文件）
  - 调用 `AiService` 进行AI分析

- **前端实现**：
  - 实现添加剪藏表单
  - 支持文本输入、类型选择、来源选择、分类选择
  - 支持手动输入标签和AI生成标签
  - 调用后端API保存剪藏内容

### 4.2 剪藏管理功能
- **后端实现**：
  - 在 `ClipController` 中实现 `getClipList` 方法
  - 调用 `ClipService.getAllClips` 获取所有剪藏
  - 实现 `deleteClip` 方法删除剪藏
  - 实现 `getDivergentSummary` 方法生成发散性总结

- **前端实现**：
  - 实现剪藏列表页面
  - 展示剪藏内容、AI分析结果和标签
  - 实现删除功能和发散性总结功能
  - 支持切换到搜索页面

### 4.3 信息检索功能
- **后端实现**：
  - 在 `ClipController` 中实现 `search` 方法
  - 调用 `SearchService.search` 进行搜索
  - 实现 `searchByCategory` 方法按分类搜索
  - 支持关键词搜索和同义词搜索

- **前端实现**：
  - 实现搜索页面
  - 支持输入关键词和选择分类
  - 展示搜索结果，包括剪藏内容和相关度
  - 支持点击搜索结果查看详情

### 4.4 内容整理功能
- **后端实现**：
  - 在 `ContentOrganizeService` 中实现 `organizeContent` 方法
  - 按分类组织剪藏内容
  - 生成分类报告和每日日报
  - 支持邮件通知功能

- **前端实现**：
  - 实现内容整理状态页面
  - 展示整理状态和存储路径
  - 支持手动触发内容整理

## 5. 测试指南

### 5.1 后端测试
- **单元测试**：使用 JUnit 5 编写单元测试
- **集成测试**：测试 API 调用和服务集成
- **性能测试**：测试 AI 分析和搜索性能

### 5.2 前端测试
- **组件测试**：使用 Jest 测试组件功能
- **E2E测试**：使用 Cypress 测试端到端流程
- **响应式测试**：测试不同屏幕尺寸的布局

## 6. 部署流程

### 6.1 后端部署
1. **构建**：运行 `mvn clean package` 构建 jar 包
2. **配置**：复制 `application_templete.yml` 为 `application.yml` 并配置
3. **运行**：运行 `java -jar clip-demo-0.0.1-SNAPSHOT.jar`

### 6.2 前端部署
1. **构建**：运行 `npm run build` 构建前端代码
2. **部署**：将 `dist` 目录部署到静态文件服务器

### 6.3 Electron打包
1. **准备**：确保前端和后端都已构建
2. **打包**：运行 `npm run electron:build`
3. **分发**：生成可执行文件和安装包

## 7. 常见问题与解决方案

### 7.1 后端问题
- **API Key 配置**：确保在 `application.yml` 中正确配置 DashScope API Key
- **文件解析失败**：检查文件格式是否支持，确保依赖已正确添加
- **AI 分析超时**：检查网络连接，考虑增加超时时间

### 7.2 前端问题
- **跨域问题**：确保后端已配置 CORS 允许前端域名
- **响应式布局**：使用媒体查询和弹性布局确保在不同设备上显示正常
- **性能问题**：使用 React.memo 和 useMemo 优化组件渲染

### 7.3 部署问题
- **端口冲突**：确保 8080 端口未被占用
- **依赖缺失**：确保所有依赖已正确安装
- **文件权限**：确保应用有读写存储目录的权限

## 8. 代码审查指南

### 8.1 审查重点
- **代码质量**：检查代码是否符合编码规范
- **功能完整性**：检查功能是否完整实现
- **错误处理**：检查是否有完善的错误处理机制
- **性能优化**：检查是否有性能瓶颈
- **安全问题**：检查是否有安全隐患

### 8.2 审查流程
1. **自我审查**：开发者在提交代码前自我审查
2. **同伴审查**：团队成员互相审查代码
3. **代码扫描**：使用静态代码分析工具扫描代码
4. **测试验证**：运行测试确保功能正常

## 9. 版本控制规范

### 9.1 分支管理
- **main**：主分支，用于发布稳定版本
- **develop**：开发分支，用于集成新功能
- **feature/**：功能分支，用于开发新功能
- **bugfix/**：修复分支，用于修复bug

### 9.2 提交规范
- **提交消息格式**：`type(scope): description`
  - **type**：feat（新功能）、fix（修复）、docs（文档）、style（样式）、refactor（重构）、test（测试）、chore（构建）
  - **scope**：功能模块或文件
  - **description**：简短描述

- **提交示例**：
  - `feat(clip): 添加剪藏功能`
  - `fix(search): 修复搜索结果排序问题`
  - `docs(readme): 更新项目说明文档`

## 10. 开发工具推荐

### 10.1 后端工具
- **IDE**：IntelliJ IDEA Ultimate
- **构建工具**：Maven
- **版本控制**：Git
- **代码分析**：SonarQube
- **测试工具**：JUnit 5, Mockito

### 10.2 前端工具
- **IDE**：VS Code
- **包管理**：npm
- **构建工具**：Vite
- **代码分析**：ESLint, Prettier
- **测试工具**：Jest, Cypress

### 10.3 协作工具
- **项目管理**：Jira
- **代码托管**：GitHub, GitLab
- **文档管理**：Confluence
- **沟通工具**：Slack, Microsoft Teams

## 11. 性能优化指南

### 11.1 后端优化
- **缓存**：使用内存缓存存储热点数据
- **异步处理**：使用 @Async 注解处理耗时操作
- **批处理**：批量处理剪藏内容，减少I/O操作
- **索引**：为搜索功能添加索引，提高搜索速度

### 11.2 前端优化
- **代码分割**：使用动态导入分割代码，减少初始加载时间
- **图片优化**：压缩图片，使用 WebP 格式
- **缓存策略**：合理使用浏览器缓存
- **状态管理**：使用 Redux 或 Context API 管理状态

### 11.3 AI 服务优化
- **请求缓存**：缓存 AI 分析结果，避免重复请求
- **批量处理**：批量发送 AI 请求，减少 API 调用次数
- **超时设置**：合理设置 AI 请求超时时间
- **错误处理**：优雅处理 AI 服务错误

## 12. 安全最佳实践

### 12.1 后端安全
- **输入验证**：对所有用户输入进行验证
- **文件安全**：对上传的文件进行类型检查和大小限制
- **API 安全**：实现 API 访问控制
- **日志安全**：避免在日志中记录敏感信息

### 12.2 前端安全
- **XSS 防护**：使用 React 的 dangerouslySetInnerHTML 时要小心
- **CSRF 防护**：实现 CSRF 令牌验证
- **敏感信息**：不在前端存储敏感信息
- **依赖安全**：定期更新依赖，避免使用有漏洞的依赖

### 12.3 数据安全
- **数据加密**：对敏感数据进行加密存储
- **备份策略**：定期备份用户数据
- **访问控制**：限制对数据的访问权限
- **审计日志**：记录数据操作日志

## 13. 监控与维护

### 13.1 监控
- **日志监控**：使用 ELK 栈收集和分析日志
- **性能监控**：监控系统性能和响应时间
- **错误监控**：监控系统错误和异常
- **用户行为监控**：分析用户使用行为

### 13.2 维护
- **定期备份**：定期备份用户数据
- **系统更新**：定期更新系统和依赖
- **漏洞修复**：及时修复安全漏洞
- **性能优化**：定期优化系统性能

## 14. 开发团队协作

### 14.1 团队角色
- **产品经理**：负责需求分析和产品规划
- **后端开发者**：负责后端服务开发
- **前端开发者**：负责前端界面开发
- **AI 工程师**：负责 AI 模型集成和优化
- **测试工程师**：负责系统测试和质量保证

### 14.2 协作流程
1. **需求分析**：产品经理与开发团队共同分析需求
2. **任务分配**：根据需求分配开发任务
3. **开发实现**：开发者按照规范实现功能
4. **代码审查**：团队成员审查代码
5. **测试验证**：测试工程师验证功能
6. **部署发布**：部署到生产环境
7. **维护迭代**：根据用户反馈进行迭代优化

## 15. 总结

剪藏项目是一个结合了 AI 技术的个人信息管理工具，通过本地存储和智能分析，帮助用户有效管理和利用碎片化信息。本指南提供了从开发环境搭建到部署维护的全面指导，希望能帮助开发团队高效、规范地完成项目开发。

在开发过程中，应始终遵循编码规范，注重代码质量和用户体验，确保系统的稳定性和安全性。同时，要保持团队协作，及时沟通和解决问题，共同推进项目的顺利进行。