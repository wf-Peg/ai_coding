【标签】学习工作

【标题】【学习工作赛道】+ 碎碎记 CutShelter — AI 驱动的全功能知识管理系统

---

## 0. 先和大家打个招呼吧

**我是谁**：一名私企程序员，平时写 Java 后端，热衷于用技术解决日常效率问题。

**我是怎么用 TRAE 把这个 Demo 做出来的**：

这个项目的起点只是一个模糊的想法：我每天浏览大量网页，收藏夹堆满了"以后再看"的链接，但从来不会回头整理。我想做一个工具，能帮我自动收集、分类、总结这些碎片信息。

最开始我连从哪里下手都不知道。我就直接跟 TRAE 说："我想做一个桌面应用，能剪藏网页内容，用 AI 自动打标签和写摘要。" 它帮我搭出了 Spring Boot 后端的骨架，Controller、Service、模型一层层自动生成。我每次只描述一个功能点，比如"帮我加个右键菜单剪藏"，它就能把 Electron 的 IPC 通信和 Chrome 扩展的 background.js 一起搞定。

最让我惊喜的是在以前光写策略模式就要花我半天，现在几句话就搞定了。

整个过程中，TRAE 帮我把"跨不过去的坎"——比如 Electron 打包嵌入式 JRE、浏览器扩展的 Manifest V3 权限配置、iframe 跨页面的 postMessage 主题同步——都变成了"一句话的事"。

---

## 1. Demo 简介

**是什么**：一款基于 Electron 的桌面端全功能知识管理系统（macOS/Windows/Linux 跨平台），搭配 Chrome 浏览器扩展。

**面向谁**：程序员、产品经理、研究者、学生——任何需要大量收集信息并希望 AI 帮忙整理的人。

**主要功能**：

| 功能 | 说明 |
|------|------|
| **智能剪藏** | 浏览器右键/快捷键一键剪藏网页，AI 自动生成摘要、标签、分类，支持发散性多角度总结 |
| **学习计划** | 输入学习主题，AI 自动生成分阶段路线图 + Mermaid 可视化 + 真实学习资源搜索（Exa 引擎），支持进度跟踪和 PDF 导出 |
| **密码管理** | 本地 DES 加密密码库，支持多密码库切换、安全审计、CSV 批量导入导出、AI 自动填充 |

**界面展示**：

- 首页：左侧待办时间线 + 右侧剪藏列表，全局搜索和分类筛选
- 学习计划：AI 生成路线图 + Mermaid 可视化 + 阶段资源/作业/实战任务
- 密码管理：DES 加密保险库，支持多密码库切换和安全审计
- 话题管理：从剪藏派生深度专题，Markdown 编辑器 + AI 知识生成
- 系统设置：多 LLM 模型切换（DashScope/DeepSeek）、主题外观、Git 同步配置

（产品截图见附件：01_首页.png ~ 05_系统设置.png）

---

## 2. Demo 创作思路

**灵感来源**：收藏夹焦虑 + 知识碎片化。每天浏览大量网页，收藏了上百个链接，但从来不回头看。现有的剪藏工具要么只存不整理，要么 AI 能力有限。

**想解决的问题**：

1. **信息收集太分散**：网页、文档、链接散落在各处，没有一个统一的入口
2. **收集之后不整理**：存了等于没存，缺乏 AI 驱动的自动分类和总结
3. **从收集到输出断链**：收集了素材，却没有工具帮我把碎片串成知识、产出文章或学习计划

**为什么做这个方向**：

我选择做**全功能集成**而非单一剪藏工具，是因为用户的真实工作流是"收集 → 思考 → 输出"的闭环。市面上不缺剪藏工具（Cubox、简悦），也不缺 AI 笔记工具（Notion AI），但缺少一个**本地优先、全链路打通**的桌面端工具。我刻意不做 SaaS，所有数据存储在本地 JSON 文件，密码库 DES 加密，用户完全掌控自己的数据。

---

## 3. Demo 体验地址

**交互式 HTML 体验包**：已打包为 ZIP 文件随帖子上传，下载后按以下步骤运行：

```bash
# 1. 确保已安装 Java 17+
java -version

# 2. 解压后进入目录
cd cut-demo

# 3. 启动后端
java -jar backend/target/clip-demo-0.0.1-SNAPSHOT.jar

# 4. 新终端启动前端
cd frontend && node server.js

# 5. 浏览器访问 http://127.0.0.1:3000
```

> 注：需要配置 DashScope 或 DeepSeek API Key 才能使用 AI 功能（设置页可配）

---

## 4. TRAE 实践过程

### 开发流程总览

整个项目从零到功能完整，完全通过 TRAE 对话式开发完成。以下是关键步骤：

| 阶段 | 任务 | 关键对话 |
|------|------|---------|
| 1 | 搭建 Spring Boot 后端骨架 + 剪藏 CRUD API | "帮我创建一个 Spring Boot 项目，实现剪藏内容的增删改查" |
| 2 | 集成 DashScope AI + 自动标签/摘要 | "给剪藏内容加上 AI 自动标签和摘要功能" |
| 3 | 创建 Electron 桌面应用 + 窗口管理 | "把这个项目包装成 Electron 桌面应用，支持系统托盘和全局快捷键" |
| 4 | 开发 Chrome 浏览器扩展 | "做一个浏览器扩展，右键菜单可以剪藏当前页面" |
| 5 | 实现多 LLM 热切换 + 自动降级 | "支持 DeepSeek 和 DashScope 切换，失败时自动降级" |
| 6 | 开发学习计划模块 + Exa 搜索 | "做一个学习计划功能，AI 生成路线图，Mermaid 画图" |
| 7 | 密码管理模块 + DES 加密 | "增加密码管理功能，本地 DES 加密存储" |
| 8 | 主题系统 + 深色模式适配 | "给所有页面加上 Notion 风格主题，支持深色模式" |
| 9 | 批量管理 + PDF 导出 + 打开目录 | "学习计划列表支持批量选择删除，详情页增加导出 PDF 和打开本地目录" |

### 开发截图

**TRAE 对话截图 1 — 多 LLM 架构设计**：

我与 TRAE 讨论了 LLM Provider 的架构设计，它帮我实现了 RoutingLlmProvider 代理模式，支持 DashScope 和 DeepSeek 双 provider 热切换 + 运行时自动降级。

**TRAE 对话截图 2 — 学习计划模块开发**：

从零开始开发学习计划模块，包括 AI 路线图生成、Mermaid 可视化、Exa 资源搜索、阶段进度跟踪，全部通过对话完成。

**TRAE 对话截图 3 — 主题系统适配**：

给所有页面统一适配 Notion 主题 + 深色模式，通过 localStorage 持久化 + postMessage 广播实现跨页面同步。

### 关键 Session ID

1476006378745587:f30e0af9774ec0e33594861e285fe5d1_69ecc500cd78edc5e35563ff.6a51f70f5713d3f627542731.6a51f70f5713d3f62754272f:TRAE Work CN.0.1.32.no_sid.no_ppe.T(2026/7/11 15:55:59)
