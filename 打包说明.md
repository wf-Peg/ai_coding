# 剪藏 - Electron 桌面应用打包指南

## 概述

通过 Electron 将前后端打包为一个桌面应用，用户双击即可启动，无需手动运行 Java 和 Node.js 服务。

## 环境要求

| 工具 | 版本要求 | 用途 |
|------|---------|------|
| Node.js | 18+ | Electron 运行环境 |
| JDK | 17+ | 编译后端 JAR + jlink 裁剪 JRE |
| Maven | 3.6+ | 后端项目构建 |

## 项目结构

```
ai_coding/
├── electron/                    # Electron 主进程
│   ├── main.js                  # 窗口管理、后端进程管理
│   ├── preload.js               # IPC 通信桥接
│   └── ...
├── scripts/
│   ├── build-jlink.bat          # [新增] Windows: jlink 裁剪 JRE
│   ├── build-jlink.sh           # [新增] macOS/Linux: jlink 裁剪 JRE
│   ├── download-jre.bat         # 备选：下载完整 JRE (无 JDK 时用)
│   └── ...
├── package.json                 # Electron 依赖和打包配置
├── build.bat                    # Windows 一键构建脚本
├── backend/                     # Spring Boot 后端
└── frontend/                    # 前端页面
```

## 快速开始

### 方式一：一键打包（推荐）

**Windows:**
```bash
build.bat
```

**macOS / Linux:**
```bash
chmod +x build.sh
./build.sh
```

脚本会自动完成：**询问是否递增版本号并更新 package.json** → 编译后端 JAR → jlink 裁剪最小化 JRE → 安装 Electron 依赖 → 打包桌面应用（安装包 + 更新包 + sha256）→ **询问是否推送到 GitHub Release**（gh 已登录时可选，未登录自动跳过）。

> 需要完整发布链路（版本号提示 + git push + GitHub Release 上传）也可直接运行 `scripts\release.bat`（Windows）/ `scripts/release.sh`（macOS/Linux），或 `npm run release:win` / `npm run release`。

### 方式二：手动分步操作

```bash
# 1. 编译后端 JAR
cd backend
mvn clean package -DskipTests
cd ..

# 2. 生成最小化 JRE（约 50MB，替代原来 316MB 的完整 JRE）
#    需要 JDK 17+（jlink 工具）
npm run build:jlink

# 3. 开发模式运行（调试用）
npm start

# 4. 打包桌面应用
npm run build:jlink:win     # Windows（含 jlink 步骤）
npm run build:win           # Windows（不含 jlink，需提前准备好 jre/ 目录）
npm run build:mac           # macOS
npm run build:linux         # Linux
```

### macOS 未签名安装

当前 macOS DMG 使用与 x64 版本相同的无签名模式，不需要 Apple Developer ID 证书或公证。首次安装时，将 `CutShelter.app` 拖入“应用程序”，然后在 Finder 中右键点击应用并选择“打开”；若系统仍拦截，可到“系统设置 → 隐私与安全性”中点击“仍要打开”。

该模式适合个人使用和内部分发。正式对外发布时仍建议改用 Developer ID 签名并完成 Apple 公证。

### 备选：无 JDK 环境时使用完整 JRE

如果构建机器没有 JDK（仅有 JRE 或没有 Java），可以用下载脚本获取完整 JRE：

```bash
# 下载完整 JRE（约 316MB）
npm run download-jre:win     # Windows
# 或
npm run download-jre:all     # 所有平台

# 然后正常打包
npm run build:win
```

## 关于 JRE 体积优化

本项目的后端是 Java Spring Boot 应用，运行时需要 JRE 环境。为了减少打包体积，做了以下优化：

| 方案 | JRE 体积 | 说明 |
|------|---------|------|
| 完整 JRE 下载 | 316 MB | 下载 Adoptium 完整 JRE，包含大量不需要的模块 |
| **jlink 裁剪（推荐）** | **~50 MB** | 只保留 Spring Boot 运行必需的 JDK 模块 |
| 不打包 JRE | 0 MB | 要求用户自行安装 JDK（不推荐，便携性差） |

**jlink 裁剪原理**：使用 JDK 自带的 `jlink` 工具，只提取运行必需的模块，去掉 `javac`、`jlink`、`jconsole`、`jshell`、`java.desktop` 等编译/调试/桌面组件。

如果运行时出现模块缺失报错，可以编辑 `scripts/build-jlink.bat` 中的 `MODULES` 列表，添加对应模块。

## 打包产物

| 平台 | 产物 | 位置 |
|------|------|------|
| Windows | NSIS 安装包 (.exe) | `dist-electron/剪藏 Setup x.x.x.exe` |
| macOS | DMG 镜像 | `dist-electron/剪藏-x.x.x.dmg` |
| Linux | AppImage | `dist-electron/剪藏-x.x.x.AppImage` |

## 首次启动流程

1. **双击运行应用** → 弹出「初始设置」窗口
2. **填写配置**：
   - DashScope API Key（必填）
   - 后端端口（默认 8081）
   - 前端端口（默认 3001）
   - 剪藏文件存储目录
   - 总结文件存储目录
3. **点击「保存并启动」** → 自动启动后端 Java 服务和前端页面
4. **进入主界面** → 正常使用剪藏功能

## 后续修改配置

启动应用后，通过菜单栏 **剪藏 → 设置**（快捷键 `Ctrl+,`）重新打开配置界面。

## 工作原理

```
┌─────────────────────────────────────────────┐
│              Electron 主进程                  │
│                                             │
│  ┌───────────────┐  ┌────────────────────┐  │
│  │  内置 HTTP     │  │  Java 子进程管理    │  │
│  │  静态服务器     │  │  (java -jar xxx)   │  │
│  │  (前端页面)     │  │                    │  │
│  │  端口: 3001    │  │  端口: 8081        │  │
│  └───────┬───────┘  └────────┬───────────┘  │
│          │                   │              │
│          ▼                   ▼              │
│  ┌───────────────────────────────────────┐  │
│  │         BrowserWindow                 │  │
│  │    加载 http://127.0.0.1:3001         │  │
│  └───────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

- Electron 主进程启动一个内置 HTTP 静态服务器来托管前端 `index.html`
- 同时通过 `child_process.spawn` 启动 Java 后端 JAR 包（优先使用 `jre/bin/java.exe`，回退到系统 Java）
- 配置信息保存在用户数据目录（`%APPDATA%/clip-demo/config/config.json`）
- `application.yml` 在运行时动态生成，无需手动配置

## 注意事项

1. **JRE 依赖**：打包后的应用自带裁剪后的 JRE（约 50MB），目标机器无需安装 Java
2. **开发模式 `npm start`**：会自动查找 `jre/bin/java.exe`，找不到时回退到系统安装的 Java
3. **API Key 安全**：配置信息存储在本地用户目录，不会上传
4. **端口冲突**：如果默认端口被占用，可在设置中修改
5. **图标替换**：可替换 `electron/icon.png` 为自定义图标（建议 512x512 PNG）
